import { Placeholder } from '@/lib/Placeholder';

export default function CaptureScreen() {
  return (
    <Placeholder
      title="Capture"
      caption="Data point capture (photos, video, voice, notes) lands in Phase F3."
      reference="§5.3 / §5.4"
    />
  );
}
