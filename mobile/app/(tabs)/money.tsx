import { Placeholder } from '@/lib/Placeholder';

export default function MoneyScreen() {
  return (
    <Placeholder
      title="Receipts"
      caption="Receipt capture + AI extraction (Claude Vision via ai-usage-tracker.ts) lands in Phase F2."
      reference="§5.11"
    />
  );
}
