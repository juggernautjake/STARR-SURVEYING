# Starr CAD: make it competitive — and close out what is still open

**Status:** IN PROGRESS · opened 2026-08-15 · consolidates every outstanding build item into one
stop-hook-executable doc.

> **Owner, 2026-08-15:**
> *"Please also just look through the CAD software. I want you to really think about layering and
> drawing and point management and layer management and what would be the most useful system for the
> CAD software to make it better. I want the CAD software to be fast and optimized, and I want it to
> compete with any other software out there. I want it to have AI fully integrated with all tools and
> measurements. We need to be able to pick a specific layer or set of points and tell AI what to do
> with them. We need tons of line and symbol and font variations and editing control. We need the
> hide and unhide management to work really well and be super intuitive. We need to be able to
> calculate lines and points really well."*
>
> *"Please make sure when we are drawing, that the order of clicks and placement of points and lines
> works well and is intuitive. Make sure that whenever we calculate or place offsets it is intuitive
> and simple."*
>
> *"Include again a sweep for the mobile styling/formatting for everything that has been built once
> you are done with everything else."*

---

## What this document is

One doc, because the work is being executed by a stop hook that takes the next unchecked slice and
builds it. Five streams are folded in:

1. **The hub** (P0) — retire Work Mode, finish widget editing on desktop and mobile, restyle the
   greeting banner. First, because it is small, self-contained, and on the screen the owner opens
   daily.
2. **Starr CAD excellence** (P1–P8) — the owner ask above. The bulk of the work.
3. **Carried-over open items** (P9) — the slices still outstanding in other planning docs, pulled
   here so nothing is stranded in a `pending/` file nobody opens.
4. **Integration points** (P9b) — every surfaced integration gets a caller, a failure path, and a
   sentence saying what it is for.
5. **The closing mobile/styling sweep** (P10) — explicitly requested to run *last*, over everything
   built by then.
6. **Closeout** (P11) — seeds applied to the live database, then pushed and merged to main.

**Two ordering rules are not negotiable.** P1 comes before P2–P8: it is the model and measurement
work every later phase writes against, and doing a UI phase first would build on a model that then
moves. And P10 comes last by definition — it sweeps what the others built.

---

## What was actually measured before planning

This section exists because the last pass over this codebase
(`completed/ADMIN_UI_ALIGNMENT_AND_LAYOUT_2026-08-14.md`) learned the hard way that roughly half of
any un-read audit is the instrument. Everything below was read in the source on 2026-08-15, not
assumed:

| Fact | Where | Why it matters |
|---|---|---|
| **51 tools** in `ToolType` | `lib/cad/types.ts:736-793` | The denominator for "AI integrated with all tools". |
| **13 AI tools** registered | `lib/cad/ai/tool-registry.ts` | AI reaches **25%** of the editor. This is the single largest gap against the owner ask. |
| **Two parallel AI action surfaces** | `lib/cad/ai/tool-registry.ts` (13 typed tools) vs `lib/cad/ai-engine/drawing-chat.ts` (its own op set, incl. `REDRAW_LAYER`, `layerName` routing) | Two vocabularies for "what the AI may do". Extending both is twice the work and guarantees drift. P8 settles this first. |
| **40 line types, 48 symbols** | `lib/cad/styles/linetype-library.ts`, `symbol-library.ts` | Line and symbol variation is in decent shape — *not* the gap the owner assumed. |
| **Fonts: one `fontFamily: string`, no library, no picker** | `lib/cad/types.ts:1233`; hard-coded `'Arial'` fallbacks throughout `CanvasViewport.tsx` | Fonts *are* the real gap in the "line/symbol/font variations" ask. |
| **Selection→AI exists, but only for two solver dialogues** | `lib/cad/ai/selection-points.ts` (Calc Point, Sketch Reconcile) | "Pick a set of points and tell AI what to do" is half-built: the plumbing exists, the general path does not. |
| **Layer + selection context already reaches the model** | `lib/cad/ai/system-prompt.ts`, `drawing-chat.ts:194-236` | The AI already knows the layer table, the active layer, and `selectedIds`. P8 is *reach*, not *context*. |
| **Visibility model is three separate flags** | `Feature.hidden?`, `hiddenSegments?: number[]`, `Layer.visible` | Hide/unhide is not one concept in the model, which is why it does not behave like one in the UI. |
| **Offsets already accept typed exact values** | `OffsetPanel.tsx`, `OnLineOffsetPanel.tsx`, `lib/cad/ai-engine/offset-resolver.ts` | Offsets are better than the ask implies; the gap is *discoverability and preview*, not capability. |

**Two conclusions worth stating up front, because they redirect the work:**

- **Line/symbol variety is not the problem; fonts and *editing control* are.** 40 line types and 48
  symbols is a real library. What is missing is a font system at all, and a consistent place to edit
  any of the three.
- **AI context is not the problem; AI reach is.** The model already knows the drawing. It just
  cannot *do* 38 of the 51 things the surveyor can do.

---

## D1 — "Compete with any other software" is not a slice, so it is decomposed into four claims

"Better than the competition" cannot be built or verified. It is replaced throughout this doc by
four claims that can be:

1. **Nothing the surveyor can do by hand is unavailable to the AI** (P8) — measured as tools
   reachable by AI ÷ 51.
2. **Every object's style is editable from one place, and the library is deep enough to draw a real
   plat** (P5) — measured as line types × symbols × fonts, and one editor per axis.
3. **Hidden things are always recoverable and always visible as a count** (P6) — measured as: no
   state exists where a feature is hidden and nothing on screen says so.
4. **The frame budget holds while drawing** (P1) — measured in `lib/cad/perf`, with a number, on a
   drawing large enough to hurt.

Anything that cannot be expressed as one of those four does not belong in this doc.

## D2 — Performance is re-measured before it is optimised, and the last number is not trusted

The CAD perf pass in `completed/` took a freeze from **269ms to 25ms per frame** by finding that
`getVisibleFeatures` was re-derived five times per frame. That number is a year-old measurement of a
different codebase; a great deal has shipped since.

**P1 opens by re-measuring, not by optimising.** The failure mode being avoided is the one this
codebase has hit repeatedly: a slice that "optimises" something already fast, while the actual
cost has moved somewhere nobody profiled. `lib/cad/perf` already exists and the admin cookie +
perf-overlay recipe is recorded in memory — the instrument is there, only the reading is stale.

## D3 — The visibility model is unified before the hide/unhide UI is touched

`Feature.hidden`, `Feature.hiddenSegments` and `Layer.visible` are three independent booleans that
all mean "you cannot see this". A UI built over three flags cannot be "super intuitive", because
"unhide everything" has to mean three different reversals and the user has no idea which one hid the
thing in front of them.

**So P6 starts in the model, not the panel.** One resolved `isVisible(feature)` with a *reason*
(`hidden-by-layer` / `hidden-by-feature` / `hidden-by-segment`), and the panel renders the reason.
Building the panel first would produce a prettier version of the current confusion.

## D4 — The two AI action vocabularies are merged before either is extended

Extending AI reach from 13 tools to 51 across *two* registries means 76 additions and a permanent
drift risk. `tool-registry.ts` is the typed, tested one and wins; `drawing-chat`'s op set is adapted
onto it.

**This is the highest-risk slice in the doc** and it is deliberately first inside P8, because every
subsequent AI slice doubles in cost if it is skipped.

## D5 — Click order is a specification, not a preference

"Intuitive" click order is settled by writing down the expected sequence for all drawing tools —
prompt, click 1, click 2, what Enter does, what Escape does, what right-click does — and then making
the tools match it. Without the written sequence there is nothing to test and every review is an
opinion.

The specification is drawn from **AutoCAD/Civil 3D convention**, because the surveyors using this
have that muscle memory, and a tool that surprises a trained user is the definition of
unintuitive here.

## D6 — Every phase ends green, and "green" includes the alignment audit

`node --env-file=.env.local scripts/ui-align-audit.mjs --routes /admin/cad --width 1440` and
`--width 390` are part of each CAD phase's exit criteria, not just P10's. CAD currently measures
**2 at both widths** (the accepted 18/22/24/28 control scale, D6j of the previous doc). A phase that
raises that number has regressed the editor's layout and is not done.

On Windows/Git Bash every audit invocation needs `MSYS_NO_PATHCONV=1`, or `/admin/cad` is rewritten
to `C:/Program Files/Git/admin/cad` and the run reports a single bogus `load` finding.

---

## The slices

Each is independently shippable and ends green. The stop hook takes the topmost unchecked box.

### P0 — The hub: retire Work Mode, finish widget editing, restyle the greeting

> **Owner, 2026-08-15:**
> *"I think that the quick actions widget kind of eliminates the need for the work mode
> functionality… Please build it all out so that we remove all of the workmode functionality for all
> roles, and just make sure we can fully edit and control and use the quick actions widget."*
> *"I still want the banner that says hello and tells the date and all of that, I just want the
> workmode functionality and any buttons that go with the workmode to be removed."*
> *"Please make sure the widget editing and control is fully fleshed out and complete on both pc and
> mobile."*
> *"Please reformat the greeting banner on the hub page for the desktop and mobile to be more
> friendly and appealing, but still professional."*

**P0 goes first** — ahead of all the CAD work — because it is small, it is on the screen the owner
opens every day, and none of it depends on anything else in this document.

#### D7 — `lib/work-mode/` is not the feature boundary, and deleting it would delete time tracking

Traced on 2026-08-15. The directory holds **two unrelated things**:

| Actually Work Mode — retire | Only *lives* in `lib/work-mode/` — must survive |
|---|---|
| `app/admin/work-mode/**` — the routes, drawer, role picker, and the five role workspaces (`field_crew`, `researcher`, `equipment_manager`, `tech_support`, `admin`, `developer`, `start`) | `clock-session.ts`, `clock-modals.tsx` — the clock the **site-wide top-bar pill** uses, on every admin page |
| `lib/work-mode/work-mode-store.ts` | `activity-tags.ts`, `use-activity-tags.ts` — also served by `/api/admin/activity-tags` |
| `app/admin/me/components/WorkModePrompt.tsx` and every entry-point button | The Quick Actions **Clock In/Out** tile |

**Clock-out POSTs to `/api/admin/time-logs`.** Those are payroll hours. A literal reading of "remove
all of the work mode functionality" — delete the directory — would remove time tracking from the
product and break the greeting banner the owner asked in the same breath to keep.

So: **retire the Work Mode destination; relocate clock + activity tags to `lib/time-tracking/`** so
the folder name stops implying a dependency that was never real.

#### D8 — The shell is disposable; the ten things inside it are not. Rehome, then delete.

**Owner decision, 2026-08-15: rehome first, then delete.** Nothing is lost.

The premise for retiring Work Mode — Quick Actions makes navigation fast — is exactly right about
the *shell*. It does not cover what the Field Crew workspace actually contains. Traced on
2026-08-15, `FieldCrewWorkspace.tsx` is **14 tabs**, and most have **no other entry point in the
product**:

| Capability | Alternative entry today |
|---|---|
| **Mileage capture** | ❌ None. The `mileage-tracker` hub widget reads `/api/admin/mileage?summary=1` — it *displays* a total and cannot record a trip. Mileage feeds financials. |
| **Job Instructions** (RPLS-authored, read + save) | ❌ None |
| **Field Notes**, **Job Media**, **Job Files** | ❌ None in this shell |
| **Field AI Assistant** | ❌ `/api/admin/work-mode/assistant` has exactly one caller |
| **Surveying Tools** (traverse / angle math), **Field Calculator** | ❌ None |
| Job picker, job summary, tap-to-call contacts | ✅ Reachable from the normal job pages |

So the deletion is **last**, not first. Each capability gets a home on a normal admin page or a hub
widget, and only then does the shell go.

| # | Slice | What it does |
|---|---|---|
| **C0a** | ✅ **DONE** — move clock + activity tags out of `lib/work-mode/` | `clock-session`, `clock-modals`, `activity-tags`, `use-activity-tags` → `lib/time-tracking/`, with their tests. Imports updated across `ClockInPill`, `HubGreeting`, the Quick Actions widget and `/api/admin/activity-tags`. It left `lib/work-mode/` holding exactly one file, which confirmed D7's split was the real boundary. |
| **C0b** | Rehome + **redesign** mileage capture — see D9 | Not a straight move: the owner has respecified it from odometer readings to addresses. Split into C0b1–C0b4 below. |

#### D9 — Mileage becomes address-based, and that is a new integration, not a rehome

> **Owner, 2026-08-15:** *"For mileage capture, I think I want to simplify. I just want it so that
> there is a manual capture. I want it so that we can put in the starting address and the job address
> and the distance will be calculated and then that will use the miles per gallon to calculate the
> cost as well. So all mileage tracking will just be manually entered for each job/trip."*

Today's capture is **odometer-based**: start reading, end reading, `resolveOdometerEntry` → miles →
dollars at `IRS_BUSINESS_RATE_2025`. The new design replaces the *input* (two addresses) and adds an
*output* (fuel cost from MPG). Three things it needs do not exist yet — checked on 2026-08-15:

| Needed | State today |
|---|---|
| Address → address distance | **Nothing.** No geocoding, no distance-matrix, no maps provider anywhere in `lib/` or `app/api/`. This is a new external integration with an API key and billing attached. |
| Miles per gallon | **No MPG field** on vehicles. Schema + seed. |
| Fuel price | No source. Either an org setting or a per-trip input. |

**The reimbursement question, which must not be answered by accident.** The IRS rate and a fuel-cost
estimate are different numbers for different purposes — one is what the firm reimburses and reports,
the other is what the trip actually cost in fuel. Quietly replacing the first with the second would
change what lands in the mileage report and downstream in `/admin/payouts/tax-report`.

**So both are kept:** distance still drives the IRS reimbursement exactly as it does now, and the
fuel cost is computed and stored *alongside* it. Nothing about the existing money path changes;
"calculate the cost as well" is served as an addition, and the owner can retire the IRS figure later
as a deliberate decision rather than a side effect.

| # | Slice | What it does |
|---|---|---|
| **C0b1** | Distance provider | Add the address→address distance integration behind one adapter, so the provider can change without touching the form. **Owner-gated: needs a maps API key and billing enabled.** Until it is, the form accepts a typed distance and the lookup is the enhancement — capture must not be blocked on a key. |
| **C0b2** | MPG on vehicles + a fuel price | Schema, seed, and the settings surface. Per-vehicle MPG, org-level fuel price with a per-trip override. |
| **C0b3** | The manual trip form, rehomed | Start address, job address, job, vehicle, date. Shows distance, IRS reimbursement **and** fuel cost before saving. Lives on a real page + the `mileage-tracker` widget gains capture, since the widget today can only read `?summary=1`. |
| **C0b4** | Retire the odometer path | Only after C0b3 is proven: remove `resolveOdometerEntry` capture from the UI, keep historical odometer-derived rows readable. Existing `mileage_entries` rows must not be orphaned by the new shape. |
| **C0c** | Rehome **job instructions** | Read + save, onto the job page where the rest of a job's detail already lives. |
| **C0d** | Rehome **field notes, job media, job files** | Same destination logic — these are per-job records and the job page is their home. Check for overlap with the existing files/media surfaces before building a second one. |
| **C0e** | Rehome the **field AI assistant** | Decide whether it survives as its own surface or folds into the existing admin AI entry point. `/api/admin/work-mode/assistant` gets renamed out of the work-mode namespace either way. |
| **C0f** | Rehome **surveying tools + field calculator** | Traverse/angle math already exists in `lib/cad` and the worker survey stack; check whether this is a third copy before moving it. |
| **C0g** | Retire the Work Mode destination | *Only now.* Delete `app/admin/work-mode/**`, `work-mode-store.ts`, `WorkModePrompt.tsx`, the `e2e/work-mode.spec.ts` and `__tests__/work-mode/**` suites, and the `WorkModeHarnessMount`. Remove the **7 route-registry entries** so the nav, ⌘K palette and the audit stop listing them, drop `/admin/work-mode` from `CHROME_BYPASS_PREFIXES` and the bundle-gate case, and remove every entry-point button **for all roles**. |
| **C0h** | Catch what pointed at it | Any remaining link, badge, notification target, or **saved custom Quick Action** whose href was `/admin/work-mode/*`. `resolveActions` drops unknown *ids*, but a custom link stores a raw href — it will 404 rather than disappear. |
| **C0i** | Verify time tracking survived | Clock in and out from the top-bar pill **and** the Quick Actions tile; confirm a `time_logs` row lands with the right hours, job and activity tags. Proves D7 was applied, not merely written down. |
| **C0j** | Greeting banner — restyle | `app/admin/me/components/HubGreeting.tsx`. Keep what it says (hello, the date, and the rest); make it friendlier and more appealing while staying professional. Desktop **and** mobile layouts, both designed rather than one reflowed. |
| **C0k** | Quick Actions — finish the editing UI | The model and editor shipped in A13 (`lib/hub/custom-quick-actions.ts` + the settings panel: label, destination from the route registry or any address, icon, colour, tooltip, add/edit/delete, href allow-list). What it still lacks: **reordering custom links against catalog ones is the only ordering control**, there is no duplicate, and the icon set is 16 fixed glyphs. Fill those in. |
| **C0l** | Widget editing — audit every widget's settings form | One pass over every registered widget: does its settings form expose everything its content model holds? The Quick Actions form is the reference for completeness. Produce the gap list before fixing. |
| **C0m** | Widget editing — close the gaps from C0l | |
| **C0n** | Widget editing on mobile | `lib/hub/components/MobileWidgetSettings.tsx` and `GridEditor.tsx` are separate surfaces from the desktop editor. Every capability available on desktop — add, remove, reorder, resize, configure — must be reachable on a phone. Drag-reorder and resize are the ones that typically have no touch equivalent. |
| **C0o** | Drive the hub end to end at both widths | Add a widget, configure it, reorder, resize, delete, add a custom Quick Action, click through to its page — at 1440 and at 390. The A13 pass found the Save button unclickable under the FAB dock and Escape not closing the options panel; neither was visible to any measurement. |

### P1 — Foundations: measure, then make it fast

| # | Slice | What it does |
|---|---|---|
| **C1** | Re-measure the frame budget | Reopen the perf overlay on a deliberately heavy drawing (≥5k features, ≥2k points). Record ms/frame while idle, while panning, while drawing, and while dragging a selection. **Output is a table of numbers in this doc**, not a fix. D2. |
| **C2** | Profile the render path against C1 | For whichever of the four states misses budget, find the actual cost. Suspects to confirm or clear: per-frame re-derivation, label layout, spatial index rebuilds, `linetype-renderer` dash generation, symbol re-rasterisation. |
| **C3** | Fix the top cost, re-measure, repeat until budget holds | One cost per commit, each with a before/after number. Stop when idle/pan/draw/drag all hold the budget. |
| **C4** | Large-drawing guard rails | Whatever C2 finds that is O(n) per frame gets a spatial or cached path so the editor degrades gracefully rather than falling off a cliff at some feature count. Record the count at which it used to fall over. |

### P2 — Layer management

| # | Slice | What it does |
|---|---|---|
| **C5** | Layer model audit | What a layer carries today (colour, visibility, lock?, print?, order?) vs what a surveying layer needs. Written up before any UI. |
| **C6** | Layer panel rebuild — the list | Search/filter, multi-select, drag-reorder, and per-layer state toggles that are all reachable without a context menu. |
| **C7** | Layer operations | Merge, split by code, move-selection-to-layer, isolate (show only this), and their inverses. Every one undoable as a single batch. |
| **C8** | Layer states / snapshots | Save a named set of layer visibilities ("field check", "client plat") and restore it. This is the feature that makes a deep layer list usable. |

### P3 — Point management

| # | Slice | What it does |
|---|---|---|
| **C9** | Point table rebuild | Sort, filter and multi-select over number / northing / easting / elevation / code / layer / description. Editable in place where the field is editable. |
| **C10** | Point selection ↔ canvas | Selecting rows selects on canvas and vice versa, including range selection by point number ("5-12") and by code. |
| **C11** | Point operations | Renumber, re-code, translate/rotate a set, derive points (midpoint, offset, intersection) — over the *selection*, undoable as one batch. |
| **C12** | Point import/export round-trip check | Confirm the formats already in `lib/cad/io` survive a round trip with codes, descriptions and elevations intact; fix what does not. |

### P4 — Drawing: click order and offsets

| # | Slice | What it does |
|---|---|---|
| **C13** | Write the click-order specification | One table: for each of the 51 tools — prompt text, click sequence, what Enter/Escape/right-click do, what the preview shows. D5. This is the artefact the next slices are tested against. |
| **C14** | Make the drawing tools match the spec | Fix every tool that deviates. Expect the deviations to cluster in the tools added last. |
| **C15** | A consistent command prompt | The status/command line always says what the tool wants next ("Select object to offset", "Specify through point"). A tool that is silent about what it wants is the root of "unintuitive". |
| **C16** | Offsets: preview and simplify | The numeric panels already accept exact values (P0 finding). Add live preview of the resulting geometry before commit, make side (left/right/both) obvious, and make the same interaction serve OFFSET and PERPENDICULAR. |
| **C17** | Snap feedback | Make the active snap and its type visible at the cursor at the moment it engages. Snapping the surveyor cannot see is snapping they do not trust. |

### P5 — Styles: lines, symbols, and the missing font system

| # | Slice | What it does |
|---|---|---|
| **C18** | Font system — the model | There is none today beyond `fontFamily: string`. Add a text-style concept (family, size, weight, slant, width factor) with a library, mirroring how line types and symbols already work. |
| **C19** | Font system — the editor and the picker | Pick and edit text styles anywhere text is placed, with a live sample. |
| **C20** | One style editor per axis, reachable from one place | Line type, symbol and text style each get an editor, and selected geometry routes to the right one. Today this is scattered. |
| **C21** | Library depth pass | Confirm the 40 line types and 48 symbols cover what a plat needs (survey, utility, topo, boundary); add what is missing. Deliberately *after* the editors, so gaps are added in the finished shape. |
| **C22** | Style by code | `lib/cad/codes` already maps codes to styles. Make the mapping editable in the UI and previewable, so a field code list drives the drawing's appearance. |

### P6 — Hide / unhide that behaves like one idea

| # | Slice | What it does |
|---|---|---|
| **C23** | Unify the visibility model | One `visibility(feature)` returning visible plus a *reason* when not. D3. Pure, tested, no UI. |
| **C24** | Hidden-items panel over the unified model | Every hidden thing in one list, grouped by reason, each with the exact control that will bring it back. |
| **C25** | Always-visible hidden count | A persistent indicator of how much is hidden, clickable to the panel. Removes the "where did my linework go" failure entirely. |
| **C26** | Isolate / unisolate as a first-class mode | Enter isolate on a selection or layer, and one obvious control to leave it — with the mode visible while it is on. |

### P7 — Calculations on lines and points

| # | Slice | What it does |
|---|---|---|
| **C27** | Inventory the calculators | What exists (`lib/cad/calculators`, curve, intersect, calc-point, inverse, closure, bowditch) against what a surveyor expects. Gap list. |
| **C28** | One calculation surface | The calculators are reachable from one predictable place, take the current selection as input, and write their result back as real geometry with provenance. |
| **C29** | Fill the gaps from C27 | Whatever the inventory says is missing. |
| **C30** | Show the work | Every calculated point/line can report how it was derived. This is what makes a calculation trustworthy in a survey deliverable, and it is what P8 needs to explain AI-authored geometry. |

### P8 — AI integrated with all tools and measurements

| # | Slice | What it does |
|---|---|---|
| **C31** | Merge the two AI action vocabularies | `tool-registry.ts` wins; `drawing-chat`'s ops adapt onto it. D4. **Highest risk in the doc; first in the phase.** |
| **C32** | Selection as AI scope | "Do this to *these*" — the current selection (points, features, or a whole layer) is an explicit, visible scope on the AI panel, not something inferred from a prompt. Extends the `selection-points.ts` path that today serves only two dialogues. |
| **C33** | Layer as AI scope | Same, for "everything on this layer". |
| **C34** | Extend AI reach: drawing tools | The DRAW_* family becomes AI-callable. Counts toward the 51 denominator. |
| **C35** | Extend AI reach: modify tools | MOVE/COPY/ROTATE/MIRROR/SCALE/ARRAY/TRIM/EXTEND/FILLET/CHAMFER/JOIN/SPLIT and the vertex operations. |
| **C36** | Extend AI reach: measurement + annotation | INVERSE, MEASURE_AREA, DIM, LIST, DRAW_TEXT — "AI integrated with all measurements". |
| **C37** | Every AI action is one undo | An AI request that touches forty features reverses in one step. The batch machinery exists (`lib/cad/ai/undo-batch.ts`); this slice proves it holds for the newly-reachable tools. |
| **C38** | Preview before apply | AI-proposed geometry is shown as a proposal that can be accepted or rejected. `lib/cad/ai/proposals.ts` and `sandbox.ts` exist — this extends them across the new reach. |
| **C39** | Reach measurement | Report tools-reachable-by-AI ÷ 51 as a number, and keep a test that fails when a new tool is added without an AI path. Claim 1 of D1, made permanent. |

### P9 — Carried over from other planning docs

Pulled here so the work is in one queue. Each keeps its original doc as the reference.

| # | Slice | What it does |
|---|---|---|
| **C40** | Payroll **S9c** | Close `POST /api/admin/payroll/runs` once nothing unique lives behind it; keep `GET` and `PUT`. From `pending/PAYROLL_HOURS_AND_EMPLOYEE_MONEY_2026-08-12.md`. |
| **C41** | Payroll **S8** — auto-transfer | **Owner-gated.** The doc's own condition is "a ledger that has been reconciling in production for a while first". Do not build until the owner says the ledger has. |
| **C42** | Research platform **S-8** | Next slice of the research buildout; read `HANDOFF` first per that doc. |
| **C43** | Mobile background uploads — runtime wiring | Pure logic shipped and tested; the device-runtime wiring is owner-device-tested. **Owner-gated.** |

> **Not pulled in:** `pending/DND_SYSTEMS_UNDER_CONSTRUCTION.md` and
> `pending/SETTINGS_PER_SYSTEM_RULES_VARIANTS_2026-07-22.md`. Both were **parked by explicit owner
> directive on 2026-07-26**. Un-parking them is an owner decision, not a consolidation side effect —
> so they are named here and left where they are.

### P9b — Every surfaced integration point actually works, and says what it does

> **Owner, 2026-08-15:** *"Please make sure that every integration point that is surfaced is fully
> fleshed out and works as intended. Please review them and make sure there is good documentation
> and that everything works as it should."*

This phase exists because **"authored but not wired" is this codebase's most common defect** — it is
recorded as such in the project's own working memory, and it has bitten in a specific, repeatable
way: the Phase I survey stack once shipped **nine slices with zero callers**, `/admin/payroll`
rendered two buttons and an error message as bare text because it styled them from a stylesheet it
never imported, and the D&D pass found three rendering-condition bugs behind a 15,000-test green
suite. A green suite does not prove a surface is reachable.

An integration point is **surfaced** if a user can see it, or another system can call it. Each one
is done when it has: a caller, a failure path, and a sentence saying what it is for.

| # | Slice | What it does |
|---|---|---|
| **C44z** | **`fieldbook_notes` is a name collision, and the job manifest still cannot read notes** | Found in C0d. `/api/admin/jobs/[id]/field-data` queries `fieldbook_notes` for `body, note_template, structured_data, data_point_id`. The live table of that name is the **learn** notes table (`title`, `content`, `module_id`, `lesson_id`, …), and **no seed in this repo ever created the shape the query expects**. The route now degrades and reports `unavailable: ["notes"]` instead of 500ing, so nothing is blocked — but a job's fieldbook notes are unreadable until somebody decides whether that table should exist separately or the query should target the real one. A schema decision, not a bug fix. |
| **C44a** | Enumerate the integration points | Build the actual list before checking any of them — CAD import/export formats (`lib/cad/io`, `lib/cad/import`, `lib/cad/export`), delivery (`lib/cad/delivery`), external integrations (`lib/cad/integrations`), the AI providers, and every API route the CAD surface calls. **The list is the deliverable**; the previous pass proved that fixing before reading wastes half the effort. |
| **C44b** | Orphan check — is anything authored but never called? | For every module in the C44a list, find its caller. Anything with none is either wired up or deleted, and which one is a decision that gets written down. `__tests__/lib-orphan-ratchet.test.ts` already exists for this shape — extend it to cover the CAD surface so the answer stays true. |
| **C44c** | Drive each surfaced integration end to end | Not "does the function work" but "can a person reach it and does it do the thing". Import a real file, export it, round-trip it, run a delivery, invoke each AI provider path. Record what actually happened. |
| **C44d** | Failure paths | Every integration gets a deliberate failure (bad file, missing key, network error) and must fail *legibly* — an error a surveyor can act on, not a blank panel or a silent no-op. This is where the "queued ≠ failed" class of bug lives. |
| **C44e** | Documentation pass | One reference doc covering each integration: what it is, what it expects, how it fails, and which env vars or owner-set values it needs to be live. Env-gated integrations get their gate named explicitly, because "configured" and "working" have been confused here before. |

### P10 — The closing mobile styling/formatting sweep

Runs **last**, over everything the phases above built. Explicitly requested.

| # | Slice | What it does |
|---|---|---|
| **C44** | Full re-sweep at 1440 and 390 | `scripts/ui-align-audit.mjs` over all routes at both widths. Fix every non-CAD finding to 0; CAD holds at its accepted 2. |
| **C45** | Drive the new surfaces on a phone | The audit cannot see a Save button under a dock or an Escape that does nothing (D5b of the previous doc). Every panel, editor and dialog built in P2–P8 gets driven at 390px. |
| **C46** | Functional sweep | `scripts/qa-sweep.ts` over all routes: nothing throws, no failed same-origin request, no error prose, no overflow. |
| **C47** | Green gate | `tsc`, `next lint`, `npm run build`, and the full vitest suite. `npm run build` is non-negotiable — it has been broken while tsc and 21k tests were green. |

### P11 — Closeout: seeds applied, then merged

> **Owner, 2026-08-15:** *"Once you are done auditing everything, fixing every issue you find, and
> you have completed all of the requests I have made, please make sure that all sql seed files have
> been applied to the supabase database, and also make sure that the changes get pushed and merged
> to main at the end."*

Runs **after P10**, and only after it. Nothing here is safe to do early: seeds applied against a
half-built schema and a merge of half-finished work are both hard to walk back.

| # | Slice | What it does |
|---|---|---|
| **C48** | Reconcile seeds against the live database | Establish which seed numbers the live DB has actually had applied, versus what is in the repo. The count is the deliverable — **do not apply anything yet.** The project's recorded method: apply with node-pg + `SUPABASE_DB_URL` (the CLI paths fail on this setup), verify through PostgREST with the service key. |
| **C49** | Apply the outstanding seeds | Apply in order, verifying each. A seed that is already applied must be recognised as such rather than re-run — several seeds in this repo are not idempotent. |
| **C50** | Verify against the live schema | Confirm the tables, columns and rows the seeds were supposed to create are actually there, through PostgREST rather than by trusting the apply step's exit code. |
| **C51** | Green gate before merge | `tsc`, `next lint`, `npm run build`, full vitest suite, and the alignment audit at both widths. `npm run build` has been broken in this repo while tsc and 21k tests were green — it is the one that catches client/server boundary breakage. |
| **C52** | Push and merge to main | **Owner instruction 2026-08-15 supersedes the standing PR-workflow preference for this initiative.** `gh` is not installed here, so if a PR is wanted instead, supply the compare URL. Confirm with the owner immediately before merging — this is the one irreversible step in the document. |

---

## Ledger

| Slice | State |
|---|---|
| **A14** CAD narrow-width (prerequisite, done) | ✅ Committed `140e37463`. 390px **28 → 2**; 1440px unchanged at 2. Three defects, not 28: both bars were non-wrapping flex rows (Save/Exit up to 156px off-screen, status strip 463px off); `overflow-hidden` on the status bar was clipping the snap popover **at every width, desktop included**; and docked panels claimed 436px of a 390px screen, leaving the canvas **zero width**. Fixed by wrapping (not `overflow-x: auto`, which would have clipped the same popovers) and by floating the docks over the canvas below 900px. |
| C0a Move clock + tags to `lib/time-tracking/` | ✅ `642349b39`. Pure move + import rewrites; tsc clean, 52 tests pass. Left `lib/work-mode/` holding one file, confirming D7's boundary. |
| C0b1 Distance provider (address→address) | ⬜ **owner-gated: maps API key + billing** |
| C0b2 MPG on vehicles + fuel price | ✅ Seed **592** (`vehicles.mpg`, seven additive columns on `mileage_entries`, an `app_settings` `mileage` row defaulting to 389¢/gal), `lib/mileage/fuel.ts` + 15 tests, and MPG wired through the vehicles API and form. **Additive by design** — `rate_cents_per_mile`/`total_cents` are untouched, so reimbursement and the tax report are unaffected (D9). Two things worth carrying forward: an unknown MPG returns `null`, never `0`, because "cannot estimate" and "cost nothing" must not render alike; and `/api/admin/settings` keys are a **whitelist**, so a seeded section that is not added there reads back fine and silently 400s on save. Historical rows were stamped `distance_source='odometer'` rather than left ambiguous. |
| C0b3 Manual trip API — **and the bug it exposed** | ✅ API half done. **Manual mileage capture had never worked.** The route wrote `total_cents` (GENERATED ALWAYS, seed 282) and `source: 'odometer'` (not in the CHECK constraint) — Postgres rejects both, so every save 500'd and `mileage_entries` held **0 rows in production**, confirmed against the live DB. Verified by round-tripping the old and new insert shapes in a rolled-back transaction: old → `cannot insert a non-DEFAULT value into column "total_cents"`, new → OK with `total_cents` computed to 2848. Route now takes a distance **or** odometer readings, snapshots MPG + fuel price, and records `distance_source`. Seed 592 applied live. **`__tests__/mileage/manual-mileage-route.test.ts` was asserting both bugs as requirements** and stayed green throughout — corrected, with a note on why a source scan cannot see a constraint violation. Form UI is the remaining half → C0b3b. |
| C0b3b Manual trip **form** UI, rehomed | ✅ `app/admin/mileage/LogTripForm.tsx` on `/admin/mileage`. **A third dead end in the same feature:** the hub widget's "Log a trip →" pointed at this page, and the page was a read-only GPS report with no form — so the CTA led nowhere you could log anything (it previously pointed at `/admin/me?tab=mileage`, which silently reloaded the Hub). Now true. Preview uses the **same `estimateTripFuel` the API uses**, so the screen and the row cannot diverge. **Driven end to end against the live DB:** 42.5 mi → 201, `total_cents` 2975 computed by Postgres, fuel 894¢ from 2.3 gal at 18.5 mpg × 389¢, `mpg_snapshot`/`fuel_price_cents` both captured; test rows and the temp vehicle deleted, 0 rows remaining. An unknown mpg renders "pick a vehicle with an mpg", never `$0.00`. Alignment audit 0 at 1440 **and** 390. |
| C0b4 Retire the odometer capture path | ⬜ |
| C0c Rehome job instructions | ✅ `app/admin/components/jobs/JobInstructionsPanel.tsx`, on the job detail page's **Field Work** tab — instructions belong to a job, not to a shell, and Field Work is who they are addressed to. The API was always job-scoped (`/api/admin/jobs/[id]/instructions`), so nothing server-side moved. **Round-tripped against the live DB on job 2026-0001:** GET 200 → PUT 200 (`brokenRefs: []`) → re-GET returned resolved segments → restored to the original empty value. `canEdit` honoured. A `job-file:` reference whose file is gone still renders a visible "missing" chip rather than a dead link — a crew needs to know the thing they were told to look at no longer exists. Alignment audit 0 at 1440 and 390. |
| C0d Rehome field notes / media / files | ✅ **Checking first showed only ONE of the three needed moving.** Job **files**: Work Mode listed the same endpoint the job page's Files tab already uploads/deletes/attaches through — strictly superior, nothing to move. Job **media**: genuinely uncovered, so `JobFieldMediaPanel` now sits on the Field Work tab. Field **notes** were `localStorage`-only, never synced → see C0d2. Fixing the panel's dependency exposed **two more live defects** (below); route now returns **200** with `unavailable: ["notes"]`. Alignment 0 at 390. |
| C0d1 `jobs` geofence columns — **the migration nobody wrote** | ✅ Seed **593**, applied live. `jobs.centroid_lat`, `centroid_lon`, `geofence_radius_m` are read by four code paths and **no seed ever created them** — seed 227's header even describes them as already populated. `/api/admin/jobs/[id]/field-data` returned `column jobs.centroid_lat does not exist`; the geofence endpoint and `/admin/timeline`'s "Set as job site" could never persist. Deliberately NOT reusing `jobs.latitude/longitude`: that is the geocoded address, while the centroid is an anchor the crew sets from where they stood. |
| C0d2 Field notes — **deferred, with the reason** | ⬜ **Deferred.** The Work Mode field-notes tab wrote to `localStorage` keyed by job and never synced to the server, so nothing durable exists to migrate and no other user could ever see one. The job page already carries `jobs.notes` and a Messages tab for notes that need to survive a device. Building a *new* per-job scratchpad is not what retiring Work Mode requires — but the old one should not be silently dropped either, so it is recorded here for an explicit owner call. |
| C0e Rehome the field AI assistant | ✅ Route moved `/api/admin/work-mode/assistant` → **`/api/admin/field-assistant`** (a path naming a shell that is going away is a comment that has started lying), and it gained a home as the **Field Assistant hub widget** — a personal utility like the calculator and mileage tracker, on the surface the owner is consolidating onto. Verified live: new path 200 with a correct answer (*"N30°E → back-azimuth 210°, S30°W"*), old path 404. **Three repo enforcement tests caught what I had missed** — `widget-options.ts` needs a registry entry AND a `settings-form` entry, and every widget must reach 1×1 (Slice 217). Exactly the "authored but not wired" guard, working. 2,015 hub/ai tests pass. |
| C0f Rehome surveying tools + calculator | ✅ **The duplication check said port it, for two reasons.** `lib/surveying/calculator.ts` holds **16 named operations** (bearing↔azimuth, angle arithmetic, triangles/trig, latitude & departure) with **exactly one consumer** — the Work Mode shell. It is *not* a third copy: CAD's `calculators/` is a generic four-function state machine for its own modal, and CAD's curve/closure/inverse tools work on drawing geometry, not typed numbers. Separately, dropping the arithmetic pad would have orphaned `lib/jobs/calc.ts`, which `__tests__/lib-orphan-ratchet.test.ts` actively tests for. So **one** `surveying-calculator` widget carries both modes — "work out a number in the field" is one job, and a surveyor shouldn't have to know which tile does bearings and which does multiplication. 1,936 hub tests pass; alignment 0 at 390. |
| C0g Retire the Work Mode destination | ✅ **Deleted — 29 files, 2,545 lines.** Routes, drawer, role picker, seven role workspaces, `work-mode-store`, `WorkModePrompt`, the e2e spec, the harness mount and the `__tests__/work-mode` suites; plus the **9 route-registry entries**, the `CHROME_BYPASS_PREFIXES` entry and the bundle-gate case. Verified on a clean production build: **all four Work Mode paths 404, all rehomed surfaces 200**, and the build output contains zero work-mode routes. Four judgement calls are recorded below rather than made silently. Full suite **23,956 passing**, `tsc`/`lint`/`npm run build` clean. |
| C0g1 `work-mode-eligibility` → `clock-eligibility` | ✅ The predicate never meant "can enter Work Mode" — it means **"is this person staff who work shifts"**, and its surviving caller is the site-wide clock-in pill. Deleting it with the shell would have removed the clock from every admin page. Moved to `lib/time-tracking/clock-eligibility.ts` as `isClockEligible`, semantics untouched, with a test pinning the seven roles because that set now gates **payroll hours**. |
| C0g2 `lib/jobs/crew.ts` deleted | ✅ Genuinely dead once the shell went — the orphan ratchet caught it, which is what it is for. Removed with its test rather than parked in `KNOWN_ORPHANS`. |
| C0g3 The rail-scannability test measured the wrong quantity | ✅ Removing 9 routes (8 of them `showInRail: false`) left the railed count **identical** but pushed the *ratio* past its 0.75 bound — the metric failed because its denominator shrank. A proxy that fails when the thing it proxies for is unchanged is measuring the wrong quantity, so it now asserts the **absolute** railed count, which is what the test's own comment argued for ("a rail with 126 items is a rail nobody scans"). |
| C0g4 Two verification traps, both self-inflicted | ✅ Recorded because they nearly produced a false "verified": (1) `next build` while `next dev` is running **corrupts `.next`** — they share it — so the served app 500s on every route while the build reports success; (2) a failed `next start` leaves a **zombie socket**, and probing that port returns the *previous* build's answers. The first pass reported 200s for deleted routes for exactly this reason. Stop the dev server, clear `.next`, rebuild, use a fresh port. |
| C0h Catch what pointed at it | ✅ **Checked the live database first: nothing was pointing at it.** All three `user_hub_layouts` rows are clean and `user_bookmarks` is empty, so no saved Quick Action, pinned page or bookmark 404s today. But the *mechanism* is real and permanent — `resolveActions` drops a retired catalog **id**, while a custom link stores a raw **href** and keeps rendering after its page is gone. So `isKnownInternalHref` now flags such a link in the settings panel. **Flags, never auto-removes:** the check cannot tell a dead link from a page simply absent from the registry, and deleting somebody's shortcut on that evidence is worse than the dead link. Query strings, fragments and dynamic children (`/admin/jobs/<id>`) all pass; the prefix guard stops `/admin/work` vouching for `/admin/work-mode`. Also corrected three comments that still described the shell as live — **`lib/auth-roles.ts` was deliberately left alone**, because it is a record of a real past build failure and rewriting its import chain would falsify history. 23,962 tests pass. |
| C0i Verify time tracking survived | ✅ **Proved against the live database, then made durable.** Hand-run on the clean production build: `/api/admin/activity-tags` → **200** with 28 tags (the moved catalog works), the exact clock-out payload both surfaces send → **201**, and the row landed in `daily_time_logs` with the right hours, date and email — then deleted. That proved the path on the day; `__tests__/time-tracking/clock-surfaces-wired.test.ts` is what keeps it true, because the risk was never that today's code is wrong but that a later refactor quietly re-points one surface and **nobody notices until payday**. It pins: no clock surface imports `lib/work-mode`, the pill and the Quick Actions tile share the *same* clock module (two clocks would let someone read as clocked-in on the hub and clocked-out in the top bar, with only one writing hours), the greeting still reports clock state, and both surfaces still POST to `/api/admin/time-logs`. Its own header records what a source scan **cannot** see — the `manual-mileage-route` lesson. |
| C0j Greeting banner restyle (desktop + mobile) | ✅ **Two of the three changes were repairs, not taste.** (1) The card was a two-column layout with an absolutely-positioned CTA; C0g deleted the button and left a **`padding-right: 14rem`** reservation guarding nothing — the third appearance of the defect this stylesheet's own M5 and U-7 notes describe. (2) **`.hub-greeting__clock-dot` was rendered by the component and styled nowhere at all** — only the deleted `work-mode-prompt` variant had rules, so the status dot has been an invisible empty span since it shipped. (3) The restyle itself: single left-aligned column, warmer three-stop gradient with a soft highlight, `clamp()` type so the heading scales instead of stepping at 640px, and the clock line promoted from grey text into a **status chip**. Date + chip share a row when there is width — desktop went **201px → 167px** of mostly-empty gradient. Deleted ~155 lines of dead CTA CSS and a 50-line mobile block that existed solely to manage it. **Screenshotted at 1440 and 390**, no overflow at either; alignment audit 0 at both; 23,947 tests pass. Three test files were rewritten rather than deleted — the CTA assertions became a guard on the *class* of bug their header identified (a mobile override inheriting a `position` it was not written for), which now cannot recur because no child of the card is absolutely positioned. |
| C0k Quick Actions — finish the editing UI | ✅ Three gaps closed, one of them not on the original list. **Duplicate**: the links a surveyor makes come in families (one page with four query strings, or one page tinted per crew) and without it the fifth is retyped and quietly diverges — `duplicateCustomAction` copies destination, icon, tint and tooltip, takes a fresh id, and caps the "copy" label so two tiles can never read identically. **Icons 16 → 60, in 7 labelled groups** mirroring the route registry's workspaces, because someone picking an icon already knows the *category* of what they are linking to; the flat list is now derived from the groups so the two cannot drift, and ★ ✓ ✎ are kept deliberately as the only glyphs the tint reaches (colour emoji ignore `color`). **Delete now asks first** — it was one irreversible click on a handful of typed fields with no undo anywhere in the hub, and the confirming id is held once for the whole list so two armed delete buttons can never coexist. 35 unit tests. **Verified in the browser as far as this slice can:** hub renders with no page errors and the catalog lists both new widgets — the per-widget options panel needs hover/edit-mode interaction to reach, which is C0o's job and is not claimed here. |
| C0l Widget settings completeness audit | ✅ **`scripts/widget-settings-audit.mjs`** — 54 widgets, set-differencing each `DEFAULTS` content model against what its options panel actually exposes (its own form, a declarative schema, or `none`). **Result: 47 complete, 6 deliberate `none`, and exactly ONE real gap** — `recent-activity.includeTypes`, which the widget reads to decide whether to show recent routes and the form never offers. The editing surface was in far better shape than assumed. **Three instrument corrections were needed to get there, and the first two produced spectacular false gap lists** — (1) the brace-walk started at the parameter destructuring `{ value, onChange }` rather than the function body, reporting *all 54* widgets as missing *every* key; (2) it keyed on directory name, but `equipment-out/` registers as `equipment-out-today`; (3) a non-greedy regex for registry entries ended at the first nested field object, so `daily-briefing`'s perfectly good schema read as absent. Each is D6b again: *read the findings before fixing any of them.* The 6 `declared-none` are honest — `widget-options.ts` says so in comments — but a `showOpenLink` the user cannot reach is still a setting they cannot reach, so they are surfaced as a decision to confirm rather than counted clean. Evidence: `qa-evidence/widget-settings-audit.json`. |
| C0m Close the widget settings gaps | ✅ **48 complete, 0 gaps.** The one gap was closed by *removing* the setting, not by adding a control — and the reasoning is the point. `ActivityType` had exactly one member, so a checkbox for `includeTypes` would have had one "off" state: **render an empty widget**. A control labelled, in effect, *"show nothing"*, when removing the tile already does that better. It was a placeholder — the form's own note said "activity log integration lands in a follow-up slice", and that follow-up shipped as the separate `activity` widget which supersedes this one. The key outlived the plan it was reserved for, the same shape as C0j's `padding-right: 14rem`. Verified safe before removing: no external consumer, and **no live hub layout stores it or even has the widget**. Made permanent by `__tests__/hub/widget-settings-reachable.test.ts`, which runs the audit and fails on any new gap — the failure is silent by construction (a widget reads `settings.foo`, nothing offers `foo`, nothing errors), so zero is worth pinning. The 6 deliberate `none` widgets are listed *by name* in that test, so quietly downgrading a widget to `none` to silence it shows up as a diff rather than a pass. |
| C0n Widget editing on mobile | ✅ **Four of the five capabilities were already there; height was not.** A phone gets a *different* editor — `MobileEditor` replaces `GridEditor` wholesale, because an 8-column drag-and-drop grid painter is unusable on a touch screen — and replacing a surface wholesale is exactly how a capability goes missing unnoticed: nothing errors, the sheet looks complete, and the only symptom is a thing you cannot do on a phone. Reorder (dnd-kit + touch sensors), add, remove and configure were covered, the last by `MobileWidgetSettings` reusing the *same three resolution paths* as desktop. **Width has no phone equivalent** — the stack is one column and `mobileSizeOverride` forces the rendered width to 2 for bucket maths regardless — but **height decides how much of a widget you see**, and a phone-only user was stuck with whatever had been set at a desk. Added a stepper (buttons, not a grip: a 40px row cannot host a resize handle distinguishable from the reorder grip beside it), 32px to clear the contract's interactive floor, **clamped to each widget's own `minSize.h`/`maxSize.h`** so a phone cannot produce a size the desktop grid would reject, writing through the *same* `setDraftWidgets` the grid painter uses. It moves **only `h`** — x/y/w are the desktop arrangement, and this sheet exists precisely because that arrangement is not visible on a phone. 11 parity tests. |
| C0o Drive the hub at 1440 + 390 | ✅ **Driven for real at both widths with Playwright.** Desktop: the two A13 defects stay fixed — Save is genuinely clickable (`elementFromPoint` at its centre returns the button, not the FAB dock covering it) and Escape closes the sheet. Phone: the customize sheet renders 6 rows, **the C0n height stepper actually works** (tapped *taller*, height went 2 → 3), buttons measure 32×32, Save sits on-screen and uncovered, and the page does not scroll horizontally. Quick Actions' editor is fully reachable from the phone sheet — added a link, it appeared in the list, and C0k's Duplicate and Delete controls are both present with the icon groups rendering. **One finding, and re-probing turned it into a smaller but real one.** "Add link" measured 1,314px down, which read as off-screen — but `.hub-msheet__body` scrolls, and after scrolling the button is clickable with `BUTTON` at its centre, so it was *not* the unreachable-Save defect. It was friction I introduced in C0k: a commit button behind sixty icons is a form people abandon. The row is now `position: sticky` — **780px, in the viewport with no scrolling at all** — and `sticky` not `fixed`, so a desktop panel that fits keeps it in normal flow. Alignment 0 at both widths; 23,971 tests pass. |
| C1 Re-measure frame budget | ✅ **Measured, no code changed** (D2). Full table in `qa-evidence/cad-frame-budget-2026-08-15.md`. Budget 16.7ms; `renderAll` p95 — **1k: 4.9ms · 50k: 11.1ms · 200k: 66.9ms (pan), max 136.7ms**. Four things it settles: (1) **the budget holds comfortably to 50,000 features** — 1.5× headroom at the size a real drawing reaches, so there is no performance problem in the range the product is used in; (2) **it breaks between 50k and 200k, worse than linearly** — 4× the features for 6× the frame time, so a knee exists in that range (C4 wants the exact count); (3) **`renderFeatures` is the cost** at 19.1ms p50 of a 28.7ms frame, but its **127.8ms p99 is a different phenomenon from its median** — a periodic stall, and a fix aimed at the median could leave the stutter untouched; (4) **the old 269→25ms figure has not rotted** — Large idle measures 24.8ms today, exactly where that pass left it. D2 was right to distrust a year-old number and wrong about the direction: C2–C4 are about raising the ceiling, not recovering lost ground. Labels are a steady secondary cost (6–9ms, ~25% of frame). |
| C2 Profile the render path | ✅ **One line is 78% of the render path.** `renderFeatures` opens by building `new Set(visibleFeatures.map(f => f.id))` — `getVisibleFeatures()` itself is reference-cached (the S2 fix, still working), but this derived Set is not, so a 200k string array and a 200k hash set are allocated and discarded **every frame regardless of what the camera did**. Instrumented with a nested `cullIdSets` marker rather than reasoned about: **Large pan p50 16.2ms of `renderFeatures`' 20.9ms (78%); Medium 3.3 of 4.6 (72%)**, scaling 4× for 4× the features — exactly C1's curve. **It explains both of C1's findings with one cause**: allocation + hashing gives the median, and churning ~400k objects per frame gives the GC pause that appears as the 127.8ms p99 (`cullIdSets` p99 alone is 39.7ms against its 16.2ms median). The defect is one of *scale mismatch* — the only consumer asks `has(id)` for ids in `pixi.featureGraphics`, which holds roughly what fits on screen, so **the set is built at the scale of the document to answer questions at the scale of the viewport**. Same shape as the bug S2 fixed. `culledIds` is viewport-sized and is **not** the target — measuring stopped that being assumed. Profile in `qa-evidence/cad-frame-budget-2026-08-15.md`. |
| C3 Fix top cost, repeat | ✅ **A 200k drawing now pans inside the frame budget.** The id Set moved into the store's visible-set cache, keyed on the same two references, built once per document version instead of once per frame. Large pan: `cullIdSets` **16.2 → 0ms**, `renderFeatures` **20.9 → 5.8**, `renderAll` p50 **32.5 → 14.8** (inside the 16.7ms budget), p95 39.8 → 18.8, and against C1's original run **max 136.7 → 19.7ms** — the GC pause is gone, exactly as the allocation theory predicted. Large idle 24.8 → 11.4. Nine invalidation tests, because this cache is *more* dangerous than the list it sits beside: the consumer does `if (!visibleIds.has(id)) g.destroy()`, so a stale set **destroys the graphics of a still-visible feature**. Derived from `getVisibleFeatures()` rather than re-filtering, so the two cannot disagree. **The guard test needed three corrections of its own** — a file-wide regex that matched `renderImageFeatures`' legitimate bucket-sized Set, a body slice that ran past the function into it, and finally a match against *its own comment* quoting the removed line. That last one is the rule `hub-greeting-fits-a-phone.test.ts` states outright: in this codebase, any check that reads source must strip comments first. **New top cost: `renderLabels`**, now 7.0ms of a 14.8ms frame — it did not get slower, everything around it got faster. |
| C4 Large-drawing guard rails | ✅ **The cliff is gone from the tested range.** The last O(document)-per-frame path was `renderLabels` building its keep-set by walking every layer-visible feature — deliberately un-culled (culling it would destroy and recreate every label on each pan), which is correct and is exactly why it was document-scale. Cached beside the others on the same key, with 8 invalidation tests; its failure mode is quieter than C3's — `if (!keepLabelIds.has(key)) txt.destroy()` makes a stale set read as **labels flickering during a pan**, easy to blame on Pixi and hard to trace to a cache. **C4 is a real but much smaller win than C3**: labels 7.0 → 5.9ms p50, `renderAll` barely moved, because what remains is the actual Pixi.Text work for labels *on screen* — viewport-sized and legitimate. **Where it falls over:** the cliff used to sit **between 50,000 and 200,000** (11.1 → 66.9ms p95, 4× over budget); now 50k pans at 6.9ms p95 and **200k at 14.9ms p50 / 17.5ms p95 — inside budget at the median, a whisker over at p95**. The editor degrades gradually across the whole tested range instead of falling off between the two. No fixture exists above 200k, so the *new* cliff is recorded as unmeasured rather than guessed at. |
| C5 Layer model audit | ✅ **Audit only, no code.** Full write-up in `qa-evidence/cad-layer-model-audit-2026-08-15.md`. **The model is not the problem — reach is.** `Layer` carries 19 fields and matches AutoCAD's Layer Properties Manager on 9 of 10 columns, plus survey-specific extras AutoCAD has no equivalent for (`autoAssignCodes`, `displayPreferences`, `rotationDeg`). But **every `updateLayer()` call in the entire CAD surface writes one of five fields** — `visible`, `locked`, `name`, `color`, `rotationDeg`. The rest are write-once-at-creation or write-never: **`lineWeight` is hard-coded `0.75` and `lineTypeId` `'SOLID'` for every layer ever created**, with no editor — so a layer cannot be dashed, which is how boundaries and easements are conventionally distinguished. **`frozen` is a real capability with no switch**: the store, the predicates and the render path all honour it (it also excludes from snap and selection, unlike `visible`), and only the UI is silent. **One genuinely missing field: plot / no-plot** — `PrintDialog` does not reference layers at all, so construction geometry has no way to be "on screen, never on paper". Two smaller notes: `autoAssignCodes` is inert (pair with C22, not here), and `sortOrder` duplicates `document.layerOrder` — two sources of truth for one thing. |
| C6 Layer panel — the list | ✅ **Checked first: search/filter, drag-reorder and the per-layer toggles already existed**, so the slice went to C5's actual finding — the properties with no editor anywhere. `LayerPropertiesDialog` now edits **line type, line weight, opacity, freeze and description**, opened from the layer context menu. **A layer can be dashed for the first time**; before this every layer in every drawing was hard-coded 0.75 SOLID at creation. `frozen` is labelled by what it *does* ("hide, and ignore for snap and selection") rather than by its field name, because it is not a second eye icon and "frozen" means nothing to someone who has not read the render code. **The wiring test caught a real ambiguity**: `layer.opacity` is *not* in the style cascade — `resolveFeatureStyle` takes opacity from the feature and defaults to 1, with no layer fallback, unlike lineTypeId/lineWeight which chain to the layer explicitly. Reading only the cascade would have declared the slider dead; `CanvasViewport` reads `layer.opacity` directly for feature *and* label alpha. Two mechanisms for one idea, now recorded. Every property is asserted **at both ends** — the editor writes it and the render path reads it — because a control over a field nothing reads is this repo's signature defect. Two guards on states that would read as bugs: opacity floors at 10% (a 0% layer is invisible but still snaps), and a stored weight outside the offered steps is preserved rather than silently snapped on open. 12 tests; suite at **24,000**. |
| C7 Layer operations | ✅ **Isolate, show-all and move-selection-to-layer already existed** (isolate is even on Shift+click, AutoCAD convention). The two genuinely missing were merge and split-by-code; **merge shipped**, split-by-code deferred → C7b. Merge folds one layer into another and deletes it: previously select-all-on-a-layer → move → delete, three operations and three undo steps, with a selection easy to get wrong on a busy drawing. **The whole thing is one undo entry, and the operation ORDER is the load-bearing part** — the layer removal is pushed *last* so undo restores it *first*; reversed, undo would re-parent features onto a layer that does not exist yet, and `getVisibleFeatures` drops those **silently** (geometry that exists, is selectable, is saved, and cannot be seen — the S13d failure by another route). The removal op carries the **whole layer**, not its id, or undo would restore it stripped of colour, line type and weight. Planner is pure (`planLayerMerge`) so the six refusals and the ordering are testable without an editor; 13 tests. A **locked source is refused** — `transferSelectionToLayer` does not check that because moving features *out* is a different question from writing *in*, but here the source is destroyed and that is what a lock is for. The panel's **P6d convention caught me**: no local `store` handle, every call through `getState()` at the moment it runs, which matters here because the loop mutates on every iteration. Suite **24,013**. |
| C7b Split a layer by code | ⬜ **Deferred to C22.** The other missing operation, but it depends on `autoAssignCodes` — which C5 found inert (set to `[]` at creation, no editor, nothing reads it). Splitting by a mapping nobody can populate would ship a button that always produces one layer. C22 ("style by code") makes that mapping real and editable; split belongs immediately after it, seen from the other end. |
| C8 Layer states / snapshots | ✅ **Named "saved views"** — capture which layers are on (plus frozen and locked) and return in one click. Stored **on the document**, because the combination that makes a client plat belongs to *that drawing*, not to whoever has it open; `layerStates` is **optional on purpose** — a required field would make every previously-saved drawing fail to load, a far worse bug than this feature is a good one, and a test covers loading a document without the key. **The decision that mattered: what to do with a layer created after the state was saved.** AutoCAD offers "turn them off". This leaves them alone, on asymmetry of harm — showing an unknown layer is visible and recoverable, while hiding one makes work the surveyor just did disappear from a control they may not connect to the disappearance, and geometry that looks deleted is this editor's most expensive failure. The plan *names* the unknown and the no-longer-existing layers rather than silently ignoring them. A state records **visibility only, never style** — carrying colour would make it a second source of truth competing with the properties C6 just made editable. `isLayerStateCurrent` marks the matching view green, and deliberately ignores both unknown and missing layers: otherwise adding any layer would mark every saved state stale. Restore rebuilds the layer map **once**, not per layer — looping `updateLayer` would invalidate the visible-set cache N times and re-derive the whole visible set on each, the exact cost P1 spent four slices removing. 25 tests; suite **24,038**. |
| C9 Point table rebuild | ✅ **Sort and text-filter already existed**, so the slice went to C9's other clause — "editable in place where the field is editable" — and the interesting work was deciding *where that is*. **`updatePoint` is a blind shallow merge**: it re-derives nothing and never touches the CAD feature the point is linked to by `featureId`. So most of `SurveyPoint` cannot be edited here without desyncing silently — `northing`/`easting` would move the point in the table and leave it where it was on the canvas (two truths, no error), `pointName` has `parsedName` derived from it and a whole `point-rename.ts` for its references, and `rawCode` drives four derived fields. Those are **C11's** operations, which can re-derive and sync. What is left derives nothing and drives nothing: **`description`, `elevation`** (the linked feature is 2D, so Z is independent of geometry) and **`monumentAction`** (a plain enum a surveyor genuinely re-classifies when a FOUND monument turns out to have been SET). **`description` also gained a column and a sort** — it was already searchable by the filter while having neither, so you could search for a note you could not read. Blank elevation clears to `null` rather than writing 0 ("no elevation" is a real state), a non-numeric entry is rejected instead of storing `NaN`, Escape abandons and Enter commits, and the cell click is stopped from reaching the row (which jumps the canvas). 15 tests **pin the boundary in both directions**, because the tempting next commit is "while we're here, make northing editable" — which would ship a desync with no symptom until somebody prints. Suite **24,053**. |
| C10 Point selection ↔ canvas | ✅ **First finding corrects C9: `PointTablePanel` is imported by NOTHING** — not CADLayout, not any panel, only its own file and tests. It is a superseded orphan; **`PointDataViewer` is the mounted points table and is strictly better** (covers every drawing POINT rather than only imported survey points, its coordinate edits actually *move* the point, and name edits route through the guarded rename flow). C9's in-place editing therefore went into a component nobody can open — real work, unreachable. The narrow lesson, recorded rather than left to be rediscovered: C6 and C7 asked "does this capability exist already"; C9 asked what the component *did* without asking whether anyone could *open* it. **"Is it mounted" is a different question from "is it good".** So C10 was built against the live viewer: **rows → canvas** (previously the only path was `bulkAskAI`, so send-to-layer, recolour and *delete* all acted on points the canvas never showed), **canvas → rows** (narrowing to the points among a mixed selection), with a `canvasSyncRef` guard so the two stores do not loop — cleared on a microtask because the store notifies synchronously and clearing inline would unset it before the effect ran. **Select by number and range** (`5-12, 20, 33-35`) reuses the existing `parsePointRangeString` rather than a second grammar, in its own box because search *filters* while this *selects*, and it **names the numbers it could not resolve** — a range that quietly selects nine of the twelve you asked for is how the wrong nine get deleted. A guard test fails if `PointTablePanel` gains an importer, so a third points table cannot appear because nobody noticed there were two. Suite **24,063**. |
| C11 Point operations | ✅ **Bulk renumber and re-code**, over the selection, each one undo batch — the two operations the viewer lacked (send-to-layer, recolour, delete and export already existed; translate/rotate are the MOVE/ROTATE tools; derive points is C29's calculators). **The alias chain is the whole difficulty and it is a real trap**: a point's number resolves as `pointNo ?? pointNumber ?? pointName ?? name`, so writing only the canonical `pointName` on a feature carrying `pointNo` renumbers it in the store while **every reader still returns the old number** — nothing errors, the point simply has two numbers depending on who asks. So renumber sweeps *every alias the feature already carries*, and deliberately **adds none it did not** (spreading the legacy shape would work against the migration retiring it). A test applies the plan and re-resolves through `pointNumberOf` to prove it. **Re-code needs no sweep, and the asymmetry is the reason**: `pointCodeOf` reads `code` *first*, so writing it always wins. Ordering is by current number ascending with unnumbered last and ties broken by id — deterministic, because undo/redo must land exactly where the original did. **Collisions with points outside the selection are named and confirmed, not silently allowed**: two points sharing a number make every number-keyed lookup ambiguous (range selection, `buildPointNoIndex`, the AI's "point 8"), and that surfaces later, elsewhere, as the wrong point. Numbers already held *inside* the selection are correctly not collisions — a set being rewritten wholesale passes through its own numbers. 15 tests; suite **24,078**. |
| C12 Point import/export round-trip | ✅ **Two real defects, both found by round-tripping rather than by reading either side.** (1) **The exported Code column was unreadable.** `PointDataViewer.bulkExport` writes six columns including **Code**; `CSVImportConfig.columns` had five and **no code index** — the importer *derives* code from the description. So a file this product exported, re-imported by this product, dropped the code and rebuilt it from the wrong field. Not fixable by config: two fields in the file, one slot in the config, so whichever you pointed at, the other was lost. Added an optional `columns.code` that wins over derivation when present, falls back when absent or out of range, and leaves the description whole (with an explicit column there is nothing to carve out). (2) **Escaped quotes were silently destroyed.** `splitLine` treated every `"` as a toggle and never emitted one, so the RFC-4180 doubling the exporter *correctly* writes turned `the "old" fence` into `the old fence` on the way back. **Neither side was wrong on its own** — a correct exporter and a correct importer that simply did not agree, which is exactly the failure a round-trip test exists to catch and neither a unit test nor a code read would have. Point number, coordinates and elevation were already intact, and a blank elevation correctly returns null rather than 0. 10 tests; suite **24,088**. |
| C13 Click-order specification | ✅ **`docs/cad-click-order-contract.md`** + `scripts/cad-click-order-audit.mjs`. **Extracted, not hand-written** — writing 51 rows of prose would have produced a document about what the tools *should* do, and C14 would then be graded against something nobody checked against the code. The spec reduces "intuitive" to **four questions** a surveyor has while holding the mouse (what does my first click do · how does this end · how do I get out · what will I get before I commit), because a feeling cannot be tested and those four can. **Measured over 58 toolbar entries: exactly ONE answers all four. 7 are silent on all four. 33 do not say how the tool ends. 56 of 58 never mention Escape, and 56 of 58 never describe a preview.** That 56 is not 56 oversights — it is one convention nobody wrote down, so nobody wrote it into a description. The contract then states the universal rules (Escape cancels from *any* state and is never a no-op; the prompt always says what is wanted next; anything about to be created is previewed) and the sequence for each of **eleven tool shapes**, drawn from AutoCAD/Civil 3D convention per D5 since a tool that surprises a trained user is the definition of unintuitive here. **What it explicitly does not claim:** this measures what the product *says*, not what it *does* — C14 checks behaviour, and the 56/58 is a strong hint that Escape and preview are inconsistently *implemented*, not merely inconsistently described. Two questions are left open for C14 to settle with the tools in front of it (double-click as a commit; right-click as commit vs context menu). |
| C14 Tools match the spec | 🟡 **Partial — the universal Escape rule is implemented; the per-tool sweep is C14b.** C13 predicted that 56-of-58 silence about Escape meant a missing *convention*, not 56 missing sentences. It did. **Every Escape handler in `CanvasViewport` was a special case** guarded on one ref — snap pick, paper-move, interactive rotate/scale, grab-node drag, offset source — and there was **no case for the commonest state in the editor, a half-drawn feature**. Pick the polyline tool, click five vertices, press Escape: *nothing happened*. `clearDrawingPoints` is called from a dozen places (on commit, on geometry too small to be real) and **from no cancel**. Added the general handler, ordered **last** so the specific cases keep their key, in **two steps** — abandon the geometry first, and only a second Escape leaves the tool, because one keypress doing both would take away the tool the surveyor is still using. PAN and SELECT are exempt (not operations to cancel out of); idle Escape clears the selection, per AutoCAD. **Verification, stated precisely:** tsc + 6 tests pin existence, ordering and the two-step sequence; the **tool returning to SELECT on the second Escape is browser-verified**; the **points-cleared branch is NOT** — synthetic clicks would not reach the PIXI canvas, and a control run (commit a polyline normally, expect an undo entry) **also** produced nothing, which is what proved the probe wrong rather than the feature. Without that control I would have reported a false pass. Browser proof of the first branch belongs with C0o-style driving. |
| C14b Per-tool sweep against the contract | ⬜ Each of the 51 tools checked against `docs/cad-click-order-contract.md` — prompt text, click count, preview. C14 fixed the one rule that spans every tool; this is the per-tool remainder, and needs a working canvas-input harness first (see the probe note above). |
| C15 Consistent command prompt | ✅ **51 of 51 tools now say what they want next; it was 22.** The prompt system already existed and was good — state-aware, per-tool, telling you what comes next — so this was a *coverage* question, not a build. **The 29 uncovered tools fell through to `'Type a command (e.g. line, polyline, move, rotate)'` — the IDLE message.** So picking Trim and reading the command line told the surveyor to type a command: not silence, which would merely be unhelpful, but **active misdirection about what the tool was waiting for**. Wording follows the C13 contract and is grouped by its **tool shapes** rather than alphabetically, which is what keeps the phrasing consistent between them — the contract paying for itself one slice later. Specific calls: pick-two tools **name which pick is which** ("Click the FIRST line, on the side you want to keep" — "click a line" twice is the same prompt twice, and the order changes the result); variable-length tools **say how they end**; `DRAW_ARC` distinguishes all three clicks; `EXTEND` says it acts on **whichever end the cursor is nearer**, which is invisible unless stated. A ratchet fails if a new `ToolType` ever lacks a case — that tool would fall back to the idle message and **nothing would look broken**, because the bar shows text, just the wrong text. 6 tests; suite **24,100**. |
| C16 Offsets: preview + simplify | ✅ **The preview and the simplification were already built; what was missing was a refusal that says anything.** Audited both offset surfaces against the slice's own brief and found live preview, LEFT/RIGHT/BOTH side selection, exact numeric entry with unit conversion + a feet echo, corner handling, and persistence across a tool switch — all present, for OFFSET *and* PERPENDICULAR, both driven from the same tool-store the canvas reads each frame. **So the slice as written was already satisfied, and re-implementing it would have been the fourth stale item in this doc.** The real defect was on the commit path of both panels, and both advertise it on the button face. `OffsetPanel.onKeyDown` called `handleApply()` unconditionally on Enter: with a blank or zero distance `applyOffsetFromPanel` returns `false`, `onCommit()` never fires, and **nothing happens** — while the Apply button beside it was already `disabled` for exactly that state. The mouse path gave a wordless signal; the keyboard path, on a button reading **"Apply (Enter)"**, gave none. `commitPerp` had three bare `return`s, the commonest being: leave length blank to "drag to set" it, never move the cursor off the start point, press **"Place (Enter)"** — projected length ≈ 0, endpoint `null`, nothing, forever. This is the failure S7b already named in this codebase: *a silent no-op is the worst version of this — the input was accepted, so there is nothing to retry.* The surveyor's next move is to press Enter harder. Fixed with **the channel each surface already uses**: inline `role="alert"` text in the panel (cleared as soon as they start typing, so a complaint never outlives its cause) plus the reason as the disabled button's `title`; and `cad:commandOutput` for the canvas tool, where its own success message already goes. Refusals are **per-condition** — "Pick the feature to offset first" vs "greater than zero", and a bad typed length vs a length never set — because those need different next moves and collapsing them sends half the surveyors to the wrong field. The guard test's first draft closed its source slice on a **banner comment it had already stripped**, so `fn` was the whole 14k-line file and 77 bare returns: the C3 lesson, third recurrence — a boundary marker must survive the stripping. 13 tests; CAD folder **3,621**. |
| C17 Snap feedback | ✅ **Two of the seven snap types were offered everywhere and produced nowhere.** The slice reads "make the active snap and its type visible at the cursor"; auditing it first turned up something larger. `SnapType` has seven members and `findSnapPoint` had a branch for five — **CENTER and PERPENDICULAR were never returned by anything**, while *both* the status-bar popover and the Settings dialog offer them as tick-boxes with confident copy ("Snap to the center of a circle, arc, or ellipse"; "**Essential** for creating right-angle connections"). Ticking either wrote it into `settings.snapTypes`, the engine looped straight past it, and the snap never fired — any drawing, any zoom, forever, with nothing erroring and the box staying ticked. **The surveyor concludes the snap is unreliable rather than absent, which is worse, because unreliable is a thing you keep trying.** Both implemented. **PERPENDICULAR is the interesting one: the foot is dropped from the point the command has already placed, not from the cursor** — that *is* the snap (you are drawing from a known point and want to meet this feature square; the cursor only chooses *which* foot). So `findSnapPoint` took an optional `fromPoint`, and with nothing placed yet the branch yields nothing rather than guessing — perpendicular to *what?* Feet are clamped to the segment (a foot past the end sits on a line that does not exist), circles are met along the radius with **both** crossings offered, and an arc rejects a crossing outside its swept span. Priority follows AutoCAD — CENTER after MIDPOINT, PERPENDICULAR before NEAREST — because **NEAREST always succeeds if anything is in range, so anything ordered after it would have been implemented and still never seen**. Defaults unchanged, so no existing drawing snaps differently. Then the slice as written: the glyphs are **not injective** — seven types over five shapes, ENDPOINT and PERPENDICULAR both squares, INTERSECTION and GRID both crosses, separable only by colour — so the type is now **named beside the glyph** from a shared `SNAP_TYPE_LABELS` map, wording pinned by test to the status bar's so one snap never has two names. One `PIXI.Text` created at init and re-used, with text and fill both guarded: this runs on every mouse move, and a Text per frame is a texture per frame, the exact GC pause C3 measured out. 23 tests (17 of them real behavioural runs — the engine is a pure function, so unlike C14/C16 there is no canvas to fake); CAD **3,644**. |
| C18 Font system — model | ✅ **The third style axis now has the same spine as the other two.** The premise held: the entire font model was `TextLabelStyle.fontFamily: string`, set per label — plus a parallel copy per layer *per label kind* in `LayerDisplayPreferences` (`bearingTextStyle`, `pointNameTextStyle`, …). **Nothing was named**, so "the bearing font" existed only as an identical set of values repeated at every place that made a label; changing it meant finding all of them, and two of them drifting apart was invisible until a plat printed. `TextStyleDefinition` + `BUILTIN_TEXT_STYLES` (22 styles across ANNOTATION / TITLE / SURVEY / TABLE) is deliberately shaped like `LineTypeDefinition` — same `id`/`name`/`category`/`isBuiltIn`/`isEditable`/`assignedCodes` — **because C20 gives all three axes one editor and C22 drives all three from field codes, and a fourth shape here would be a fourth thing for both to special-case.** Styles are named for the job they do ("Bearing & Distance", not "Arial 8 Condensed"), and sized in **points on paper** so they hold at any plot scale. Two decisions carry weight. **Colour is deliberately not part of a text style** — AutoCAD's STYLE carries typography only, and adding colour would give every label two places that set it and no rule for which wins. And **width factor and oblique angle are separate axes from italic**: italic swaps in a differently drawn typeface, oblique shears the upright one, which is why the cartographic water convention (`HYDROGRAPHY`) can only be expressed with both. On resolution, the named style is the **authority** for its five axes rather than a fallback under label overrides — every field on `TextLabelStyle` is required and always populated, so "the surveyor chose Arial" is indistinguishable from "Arial is the default", and a label-wins rule would mean the style never applied to anything. A dangling `textStyleId` leaves the label untouched: the drawing renders and the picker shows the unknown style, rather than a page silently re-fonted. `customTextStyles` is **optional on the document** for the C8 reason — a required key makes every previously-saved drawing fail to load. **Wired, not shelved:** the canvas resolves once before reading any font field (and applies width/oblique on the display object, not the TextStyle, so neither re-rasterises the texture on a per-frame path), and **both exporters resolve too** — the DXF writer emits the resolved font, builds its STYLE *table* from resolved fonts (otherwise the table omits an entry per styled label and the reading package silently substitutes), and carries width factor + oblique natively as groups 41/51, written only when non-default so existing exports stay byte-identical. This slice explicitly did not repeat the shape this doc has now recorded four times — real work with no caller. 28 tests; full suite **24,164**. |
| C19 Font system — editor + picker | ✅ **C18 shipped a model nothing could select from.** The library, the resolver and all three consumers were live, but **nothing could set `textStyleId`** — so every label in every drawing still carried none and every one of those paths took its "no named style" branch forever. `TextStylePicker` is one component mounted at each place a `TextLabelStyle` is edited, so "which font is this" is asked and answered identically everywhere, which is the entire point of naming styles. Grouped by category (22 styles need it), with a **live sample rendering the actual resolved typography** rather than describing it — including the two axes CSS has no font property for, via `scaleX` and `skewX`. **The skew is negated, exactly as the canvas negates its Pixi skew**, because both coordinate systems have y growing downward and a positive skew leans the glyph's top *left*; a sample leaning the opposite way from the drawing would be worse than no sample, since the surveyor would pick the style that looked right and get its mirror on the plat. Two decisions carry the slice. **Detaching to "Custom" bakes the resolved values in before dropping the id** — without that, clearing the style snaps the text back to the fields underneath (usually untouched defaults), so the label visibly jumps to Arial 10 at the very moment the surveyor chose Custom *in order to tweak it*; detach has to mean "keep this and let me edit it". And **the five axes a style owns are disabled, not left live** — an editable control that changes nothing on the canvas is the silent no-op C16 spent a slice removing — while still **displaying the resolved value**, because a control showing the raw field beside a canvas drawing the resolved one is the two-truths bug C9 refused to ship for point coordinates. A dangling reference renders as "*id* (missing)" rather than quietly reverting to Custom, so the surveyor can see the drawing points at something deleted. Mounted inside the shared `TextStyleEditor`, so **all eight per-layer label kinds** (bearing, distance, point name/code/description/elevation/coordinate, area) get it from one insertion — the drift those eight copies could develop is exactly what names prevent. 19 tests, the picker's own logic rendered for real through `react-dom/server`; CAD **3,691**. |
| C20 One style editor per axis | ✅ **The audit found the scatter was two specific holes, not general disorder.** Line types were complete (`LineTypePicker` → `LineTypeEditor`, plus add/update/remove store actions) and symbols were reachable from both PropertyPanel and CodeStylePanel. What was missing: (1) **text styles had a picker and no editor**, so `document.customTextStyles` was a field nothing could write and C19's picker could only ever offer the 22 built-ins — the same "authored but not reachable" shape C19 had just fixed one level up; and (2) **a selected TEXT feature had no style control at all**. Both closed. `TextStyleEditorModal` mirrors `LineTypeEditor` exactly — same frame, same clone-a-built-in behaviour, same add-vs-update split — with a store trio that mirrors the line-type trio including its normalisation, so **a copy of a built-in can never come back claiming to BE the built-in** (which would be uneditable and undeletable, sitting in the drawing forever). Reached from the picker's own +/✎/🗑, because "reachable from one place" fails if text is the axis you have to go somewhere else for; the newly saved style is **selected immediately**, or the surveyor creates one and then has to go find it in the list they just left. Delete counts **labels, not features** first (one feature commonly carries a bearing and a distance label following different styles) — deleting is not destructive since a dangling reference renders "(missing)", but *"delete this?"* and *"delete this, used by 412 labels?"* are different questions and only one is answerable. **The routing half turned up the third place fonts live**: a TEXT feature keeps its font in the untyped `properties` bag, so "give this the Plat Title style" worked on a bearing label and not on the plat's actual title. `resolveTextFeatureStyle` reads the same `textStyleId` key, and the canvas, the PDF writer and PropertyPanel all go through it — with the **same** picker component as the labels, since a second text-style control would drift from the first, which is the entire thing naming styles prevents. Detaching there **removes the key** rather than writing an empty one (the bag is `Record<string, string|number|boolean>` — no slot for "explicitly none", and a leftover `''` would resolve as a dangling reference the picker then reports as missing) and bakes the typography in, the same rule C19 established. One pre-existing source-locked assertion in `pdf-writer-framing` followed the moved line rather than being dropped; the `?? 12` fallback is unchanged and is now pinned behaviourally instead. 29 tests; full suite **24,212**. |
| C21 Library depth pass | ✅ **The counts were right and the coverage was not. Line types 40 → 76, symbols 48 → 84.** The plan's numbers held exactly; the *distribution* was the finding. Of the 40 line types: 9 basic dash patterns, 9 symbol-in-line patterns, 12 fences, 5 "utility" entries **four of which were shot-marker helpers rather than utility runs**, and 5 specialty. Which is to say **a boundary-survey product had no boundary line types** — no section line, no quarter-section, no right-of-way, no easement of any kind, no setback, no lot line, no tie line — and no topo ones: no contour, no edge of pavement, no bank, no tree line. A surveyor drawing an easement picked "Dashed" and remembered what they meant by it: **the un-named-style problem C18 had just fixed for fonts, one axis over.** Two new categories, `BOUNDARY` (12) and `TOPO` (13), plus 11 real utility runs. Weights encode the convention that *is* the reading order of a plat — the subject tract is the heaviest line on the sheet and a **tie line is the lightest, because a tie is a measurement and must never read as a boundary** on a plat somebody may rely on; index contours outweigh intermediates because that difference is how a contour map is read at all. Each utility run carries **both** an APWA colour (the code a locator paints the ground with — a plat that disagrees with the paint is worse than one with no colour) **and an inline letter**, because colour alone fails the moment the plat prints in black and white, which is how a plat is filed; that required 9 new inline letter symbols. Force main is patterned distinctly from gravity sewer despite sharing colour and letter, because they fail differently and a contractor digging near one needs to know which. On the symbol side, 25 of 48 were monuments while **`STRUCTURE` and `CURVE` were declared in the category union with zero members** — headings the picker already drew and then had nothing to put under, which reads as "this drawing has none" rather than "this product has none". Both filled, plus 10 utility appurtenances, 3 vegetation, 2 control. **The tests are integrity, not inventory**, and they earned it: the code-collision check caught **four assigned codes I had duplicated onto existing entries** — under C22 one claimant would silently never apply, and which one would depend on array order. Also pinned: no line type points at a symbol id that does not exist (a dangling one draws the dashes and simply no marks, throwing nothing), no declared category is empty, no dash segment is ≤ 0. The picker's category order now leads with BOUNDARY and TOPO; the type change to `LineTypeDefinition['category']` made `tsc` demand both new labels, which is exactly how a `Record<union, …>` should behave. 79 tests; full suite **24,291**. |
| C22 Style by code | ✅ **The slice was written backwards, and the half it asked for already existed.** It reads "make the mapping editable in the UI and previewable" — and `CodeStylePanel` was already a complete table: search, a symbol picker per row, a line-type picker with a live dash preview, a colour swatch, a layer select, per-row and global reset, an amber dot on every modified cell, reachable from the menu bar and persisted to localStorage. **What was not true is that any of it did anything.** `resolveCodeStyleMapping` — the one function answering "what style does this code get" — had **zero callers outside the barrel re-export**. So did `resolveStyle`, the file that calls itself the *"4-tier style resolution engine"* and whose documented tier 2 is the point-code default. The canvas resolved feature → layer → constant, with no code tier at all. **A surveyor could open the panel, set FN01 to Barbed Wire in red, close it, and watch the drawing not change — forever, with nothing logged and nothing thrown.** The editor was the half that existed; the connect was the work. `code-style-resolve.ts` supplies the tier, and it is a new module rather than a direct call for a measured reason: `resolveCodeStyleMapping` rebuilds a **134-code Map on every invocation**, fine for a panel that renders once and fatal for a render loop asking per feature per frame — the allocation-per-frame shape C3 measured out of `cullIdSets`. The default map is memoised (derived from a frozen module constant, so it cannot go stale) and the overrides are read **once per frame in `renderAll`**, not once per feature. Two matching subtleties, both tested: codes resolve by **either the alpha or the numeric form**, because a crew types `FN01` and a collector emits `740` for the same thing; and overrides are keyed by `codeAlpha`, so an alpha-keyed edit is applied to numeric-coded features too — without that, one edit would style hand-typed features and **silently skip every one that arrived as a number, from the same drawing**. Line type, colour, weight, symbol and symbol size now fall through feature → code → layer, and a test walks every `codeStyle?.` reference asserting a `feature.style.` guard precedes it, because **a code tier placed first would overwrite every deliberate per-feature edit the surveyor has ever made** — a far worse bug than the one being fixed. Finally the third axis: `CodeStyleMapping.textStyleId` (optional, so old persisted overrides still match) plus a Text Style column, wired under the label's own choice, since C18–C21 gave text a model, a picker and an editor and without this a code would drive appearance on two axes out of three. One C18 assertion that pinned the exact `resolveTextLabelStyle(label.style, …)` call shape was updated to keep its invariant while allowing the new tier. 25 tests; full suite **24,316**. |
| C23 Unify the visibility model | ✅ **Five ways to be invisible, not three — and the fifth is the one nothing calls "hidden".** D3 listed `Feature.hidden`, `hiddenSegments` and `Layer.visible`; the audit found `Layer.frozen` and, more importantly, **`Layer.opacity = 0`** — fully transparent, with the layer's eye icon still lit, which makes it the most confusing of the lot. `lib/cad/visibility.ts` returns a reason (`layer-frozen` / `layer-hidden` / `layer-transparent` / `feature-hidden`) plus the layer it lives on. Three decisions carry the slice. **Reason PRIORITY is a product decision, not an implementation detail**: a feature can be suppressed several ways at once, and the reason returned has to be the one whose reversal must happen *first* — unhiding a feature whose layer is still frozen changes nothing on screen, and a panel offering that button would be the third thing in a row that appeared to do nothing. **Partial edge hiding is deliberately NOT "hidden"**: a polygon with one boundary edge suppressed still fills its full area and still reads as present, so calling it hidden would put every fill-styled polygon in the hidden-items panel — which is how a panel meant to reduce confusion becomes the confusion. And **`wouldRevealCount` simulates one reversal at a time**, because a feature that is individually hidden *and* on a frozen layer sits in the frozen group but will not come back when the layer thaws; promising "this brings back 3" and delivering 2 costs exactly the trust C24 is meant to build. On unification: `getVisibleFeatures` now delegates to the model instead of re-implementing the flags, with **behaviour unchanged** — the one divergence is named and tested rather than duplicated. `layer-transparent` is absent from `RENDER_SUPPRESSING_REASONS` on purpose: a 0-opacity layer's features draw at alpha 0 but stay hit-testable, snappable and rubber-band selectable, and dropping them from the render set would drop them from all three, since every consumer shares that predicate. That is a behaviour change, not a model unification, and it belongs in C24–C26 where the surveyor can see it happen. Pure, no UI, per the slice. 23 tests; full suite **24,339**. |
| C24 Hidden-items panel | ✅ **The panel already existed and reported "No hidden features." while a frozen layer swallowed the drawing.** 320 lines, two tabs, per-item attributes, per-item Show, an Unhide All, reachable from the layer panel and a status-bar pill — and it filtered `f.hidden === true` **and nothing else**. So the one screen built to answer "where did my linework go" gave the wrong answer, confidently, with a matching "0 hidden" in the status bar. **That is D3's prediction word for word**: a panel over one of five flags cannot say what to undo, because it cannot see four of the ways things got hidden. Extended rather than replaced — the existing per-item rows are good and are kept for the `feature-hidden` group, where each item was chosen individually and is worth inspecting; layer-level groups get one card instead, because listing a frozen layer's 4,000 features individually is what makes a panel unreadable. Each card carries the reversal its label promises (a button reading "Thaw the layer" that flipped `visible` would be indistinguishable from a broken panel) and the count of what that reversal will **actually** reveal, with the shortfall spelled out when it differs. **"Unhide All" now unhides all** — it previously walked only the per-feature flag and the labels, so a button by that name left a frozen layer frozen, the worst kind of control because it appears to have worked and the drawing is still missing; it flips the underlying fields outright rather than iterating a group list that each reversal invalidates. The status-bar count was fixed to match, since a bar reading "0 hidden" beside a panel listing 4,012 is worse than either alone. **Building it found a sixth mechanism C23 missed**: `TextLabel.visible === false` hides a bearing call while its line stays put — not on a feature at all, which is why the enumeration missed it and why the old panel had a whole tab for it. Added as `hiddenLabels`, deliberately excluding labels on already-invisible features (un-hiding one would reveal nothing — the same "third thing that did nothing" reason priority exists to prevent). Two process notes worth keeping: I **overwrote the existing panel with `Write` before reading it** and restored it from git — the file was there, and the tool warned me by saying "updated" rather than "created". And the inline-hex ratchet caught new copy-pasted hexes twice — the real ones, then **the comment explaining the fix, which quoted the literal and counted itself**, the same self-match that cost C3's guard three revisions. 16 tests; full suite **24,355**. |
| C25 Always-visible hidden count | ✅ **The pill was already persistent and clickable — and it only works for a surveyor who thinks to look at the status bar.** C24 had made the count honest; the audit's finding here was about *reach*, not correctness. **The person most likely to miss a small amber number in the corner is the one staring at an empty canvas** — and that is exactly when the answer matters most, because a drawing opened with a saved layer state that turns everything off looks identical to a drawing that failed to load. So `BlankCanvasNotice` renders **on the canvas** when the document has features and none of them are visible: the count, what is doing it, and a button to the panel. It is **deliberately not shown for a partially hidden drawing** — hiding things is normal drafting, a warning that appeared whenever anything was hidden would be on screen most of the time, and a warning you see constantly is one you stop reading; the pill covers that case and this covers the case the pill cannot. The pill itself now carries the **breakdown rather than a bare number** ("4,012 hidden" and "4,012 hidden, all of it one frozen layer" send the surveyor to completely different places) and turns red when nothing at all is on screen, where it stops being a stat and becomes the answer to the question being asked. It still hides itself at zero — a permanent "0 hidden" is clutter, and clutter is what makes a pill easy to miss when it finally matters. `hiddenSummary` walks the drawing **once**, with a test pinning that: `countHidden`, `hiddenLabels` and `groupHidden` each iterate the full feature map and the status bar recomputes on every store change — the same reasoning as C22's per-frame read, cheap in a fixture and not on 200k features. The overlay is `pointer-events-none` with only its card interactive, since an overlay that ate canvas clicks would be worst in exactly the state where the surveyor wants to click a layer back on. One C24 assertion pinning `countAllHidden` was updated to follow the call — same invariant, same total. 19 tests; full suite **24,374**. |
| C26 Isolate as a mode | ✅ **Isolate was a one-way destructive write with no way back — and "Show All Layers" was not the way back.** Three entry points (layer context menu, Shift+click the eye, isolate-by-selection) all ran the same snapshot-less loop: `updateLayer(id, { visible: id === target })` across every layer, remembering nothing. The only exit turns **every** layer on, so **a surveyor who had deliberately switched three layers off, isolated the boundary, and came back silently got those three layers back too** — un-isolating restored something else, with no error and no way to tell. And nothing said isolate was on, which is the state C25's blank-canvas notice had already named: "an isolate left on from yesterday looks identical to a drawing that failed to load." Entering now snapshots the visibility it overwrites and leaving puts it back exactly. **Two traps, both invisible until a surveyor loses work to them, and both the reason this is a model file rather than three lines in a menu handler.** (1) **Re-isolating must not re-snapshot**: isolate A → isolate B → exit would otherwise restore the *A-isolate* — an intermediate state the surveyor never chose to keep — and their real layer state is gone for good; the existing session's `previous` is carried forward untouched, so exit always means "before any of this started". (2) **A layer created during isolate is not in the snapshot and is left alone**, the same asymmetry-of-harm C8 reasoned through: a layer that stays visible is a nuisance they can see and fix, one that vanishes on exit looks like the isolate ate their new work. The session lives **on the document**, like `layerStates` — layer visibility is saved with the drawing, so an isolate survives a reload whether or not we record it, and an in-memory snapshot would leave the surveyor isolated with no way back, *this slice's own bug made worse by looking fixed until they close the tab*. Optional field, same C8 loading reason. `enterIsolate` writes visibility and session in **one `set`**, because a two-step version interrupted between them leaves a snapshot describing a state the document is no longer in — worse than no snapshot, since exit would then "restore" something that never existed. The badge names what is isolated (not just "Isolated" — that does not tell you what you are looking at), flags layers added since, and **renders nothing once the session goes stale**: a surveyor can leave the long way round by clicking an eye back on, after which the badge would be lying and its Exit button would *hide* a layer they had just chosen to show. 28 tests; full suite **24,402**. **P6 complete.** |
| C27 Inventory the calculators | ✅ **Audit only, no code.** Full write-up in `docs/planning/qa-evidence/cad-calculator-inventory-2026-08-15.md`, measured by `scripts/cad-calculator-audit.mjs` — extracted rather than hand-written, per C13. 13 calculation surfaces, 20 engine modules, three questions each (reachable · takes the selection · writes geometry back), because those are the three ways a calculator can be useless. **The instrument needed four corrections and every one produced a confident wrong finding first**, which is the most transferable thing in the slice: (1) reachability was not transitive, so four surfaces mounted inside `CalculatorModal`/`TraversePanel` — the normal shape of a modal with tabs — came back "unreachable"; (2) write-back looked only for direct store writes, so `CalcPointDialog`, which routes through the AI review queue and is therefore *stricter*, came back "answer never becomes geometry"; (3) the lib scan was not recursive, so **`computeClosure`, `bowditchAdjustment` and `transitAdjustment` — the engines this plan names by name — came back orphaned** when they are called one directory down, a headline finding built entirely out of the instrument's blind spot; (4) fixing that made the scan self-match each engine's own `export function` line and report zero orphans everywhere, the same trap that cost C3's guard three revisions and C24's inline-hex fix a second pass. **Findings.** Only **1 of 13** surfaces records provenance — a calculated point that cannot say what it was derived from is indistinguishable from one somebody typed, which is precisely the distinction a survey deliverable turns on, and the field does not exist on `Feature` at all, so C30 starts in the model the way P6 did. `compound-curve.ts` exports **compound, reverse and clothoid-spiral solvers that nothing anywhere references** — ordinary road-alignment work, written and tested and unreachable, and the largest item for C29 by value per unit of work. **Two COGO implementations** (`cogo.ts` returning both intersection solutions vs `solver.ts`, which is what the dialog actually uses), the drift risk D4 named for the AI registries. Only **one** surface reads the selection, so C28's "take the current selection as input" is twelve surfaces of work. And `CurveCalculator` (320 lines) overlaps `CurveCalculatorBody` (272) as two separate front doors, which C28 has to resolve before adding to either. Against a standard COGO toolkit the genuinely absent items are **station–offset, radial stakeout, slope/grade/vertical curve, and partition-a-parcel-to-an-area** — the last being the classic reason a surveyor opens a calculator at all. Suite unchanged at **24,402** (no behaviour touched). |
| C28 One calculation surface | 🟡 **Clause 1 shipped (one predictable place); clauses 2 and 3 are C27-measured work that belongs elsewhere and are carried forward, not silently dropped.** C27 measured 13 calculation surfaces behind **seven different doors** — a Calculator modal with a two-entry picker, a separate legacy Curve modal, Calc Point under the *AI* submenu, Intersect under Edit, a Traverse panel and viewer under View, a Closure report nested inside the traverse panel, and an area HUD that exists only while MEASURE_AREA is active. Every one works. The problem is that a surveyor has to already know which door a given computation lives behind, and **the picker that looks like the answer offered two of the thirteen.** `lib/cad/calculators/registry.ts` is now one list of every calculation with how to reach it; the picker renders all of them, launching the ones with their own dialog by event and closing the hub so it is not sitting on top of what it just opened. **Deliberately not a rehoming**: moving twelve working dialogs into one modal is a rewrite that risks all of them to fix a *discovery* problem — the door was missing, not the rooms. The menu entry is renamed "Calculations… (all calculators)", because "Calculator…" reads like a pocket calculator and that is precisely why nobody looked there for Calc Point. C27's F5 is resolved as a **decision rather than a merge**: `CurveCalculatorBody` and `CurveCalculator` are not duplicates — the standalone one carries the `onPlace` flow and is the *only* curve path that creates geometry — so both are listed, named for what they do. The registry also carries `usesSelection` and `writesGeometry` per entry, so C27's "1 of 13 reads the selection" stays visible instead of being assumed closed because C28 shipped. **The `every-dialog-can-be-opened` guard fired, correctly, and this was its third false-finding shape**: the picker dispatches `new CustomEvent(entry.openEvent)`, so the literal lives in `lib/` and a real working dispatcher was invisible to a scan of `app/admin/cad` — same class as the two-line dispatch that fooled it before. The scan was widened rather than the finding waived. Four pre-existing source-locked assertions followed the list to its new home. **Deferred with reasons**: *selection-as-input* → C29, because C27 measured it at 1 of 13 and it is twelve surfaces of work, not a clause of this slice; *provenance on write-back* → C30, because `Feature` has no field to write it to and C30 starts in the model the way P6 did. 23 tests; full suite **24,425**. |
| C29 Fill calculator gaps | 🟡 **Gaps 1 and 6 shipped; the rest of C27's list is itemised below and carried, not dropped.** Started at the top of the list and immediately found something worse: **`CurveCalculator`'s "Place on drawing" button has never rendered.** It is gated on an optional `onPlace` prop, and nothing has ever passed one — so the single curve path in the product that creates geometry did not, and **C28's registry asserted `writesGeometry: true` for it on the strength of the prop existing in the type**. Worth stating plainly, because it is a class of error rather than a typo: a capability can be present in the signature and absent from the running product, and no amount of type-reading finds it. Wired now, undoable, on the active layer, through a pure `curveToFeature`. **The conversion had a real bug, and the test written to catch it caught it.** Mapping `anticlockwise` from the curve's direction by *reasoning about the convention* — "deflecting RIGHT sweeps clockwise, so LEFT is anticlockwise" — is **backwards for this solver**, and that is what shipped in the first draft. Measured instead: RIGHT runs −180° → −120° (increasing, anticlockwise), LEFT runs 0° → −60° (decreasing, clockwise). Backwards draws the **major arc** — 1047 ft of arc where 209 was solved, a 300-foot error that still passes for a curve at a glance. The sweep is now asserted in both directions, from the geometry's own flag rather than from the curve's direction, so the assertion cannot smuggle in the assumption it is checking; the first draft of the test managed to be wrong and self-consistent at once. Then the three engines: `compound-curve.ts` had **no tests at all** — the repo already listed it as "built ahead of a UI that can express it", so the UI C29 owes it would have been built on arithmetic nobody had checked. 14 tests now pin the properties that define each one: the compound curve's two arcs **join and stay tangent through the junction** (the unit slip that would break it is real — `computeCurve` takes degrees while `CurveParameters.tangentOutBearing` is radians, and the engine converts); the reverse curve **actually reverses**, checked both by the direction flag and independently by putting the two radius points on opposite sides of the common tangent, so a flag that lied would still be caught; the spiral satisfies **A² = R × L**, starts at infinite radius, and deflects to the correct side — a sign error there is invisible in every scalar. All three pass. **Remaining C29 gaps, unchanged from C27's list**: station–offset; radial stakeout; slope/grade/vertical curve; partition-a-parcel-to-an-area; picking one of the two COGO vocabularies; and C28's deferred selection-as-input across twelve surfaces. 25 tests; suite **24,450**.<br><br>**C29b — the compound/reverse/spiral UI. ✅** `AdvancedCurveCalculator`, registered inline in the C28 hub, solving all three and **drawing them**. The spiral needed a tessellator: `computeClothoidSpiral` returns only TS and SC, and **a clothoid cannot be faked with an arc** — its radius varies along its length, which is the entire reason the curve exists. `spiralPolyline` samples the **same truncated series the solver uses**, at `s ≤ L` instead of only at `L`, deliberately: a different (even a better) expansion would put a polyline on the drawing that does not end at the SC point the curve table reports, and a curve table disagreeing with the geometry beside it is worse than either alone. A test pins that the last sample *is* `sc`, in both directions and off an arbitrary bearing, plus that the curve actually curves (a collinear tessellation passes every endpoint check and draws a chord) and that its deflection grows monotonically, since a clothoid's curvature increases from zero and a series error shows up as a wobble. Placement is **one undo entry** for both arcs: two that arrived together must leave together, or undoing half a compound curve leaves geometry that is neither a curve nor nothing. Impossible input is refused with a reason rather than a dimmed button (the C16 rule) — `A = √(R·L)` goes imaginary on a non-positive radius or length, and placing NaN coordinates puts geometry at nowhere, which is far harder to notice than a blocked button. **Both orphan ratchets fired on the same run and were right**: `compound-curve.ts` had been recorded as "built ahead of a UI that can express it", and building it made the entry stale. Removed from both, which is the outcome that note was hoping for rather than excusing. 18 tests; full suite **24,468**.<br><br>**C29c — station–offset and radial stakeout. ✅** Two of the four capabilities C27 found genuinely absent, and the two a crew uses on an ordinary day. Pure engine in `lib/cad/geometry/stakeout.ts`, and the tests lean hard on **sign**, because a stakeout number wrong by a sign sends somebody to the wrong side of a road while every scalar in the answer still looks right. Three decisions carry it. **The nearest segment wins, not the first within tolerance** — an alignment that doubles back (a cul-de-sac, a hairpin) has points close to two segments at once, and taking the first reports a station hundreds of feet out while the offset still looks plausible; ties break toward the earlier segment so a point at an interior vertex never oscillates between two answers. **Stations past the end clamp rather than extrapolate**: a surveyor typing 2400 on a 190-foot alignment has made a mistake, and inventing centreline to honour it answers the question they typed instead of the one they meant. And **a target sitting on the setup is dropped, not reported** — `atan2(0,0)` is 0, which would print `N 00°00'00" E, 0.00 ft` and read like a real shot at north. `shotToPoint` closes the loop so a stakeout list can be **checked against the drawing it came from** rather than trusted. The UI is also **the first surface built to C28's second clause**: it reads the *live* selection, subscribing to `selectedIds` **and** to `document.features` — without the first the list freezes at whatever was selected when the modal opened, without the second moving a staked point would not change its distance. That takes C27's selection-as-input count from **1 of 13 to 2**, and the registry test was changed from a fixed list to a ratchet so future surfaces doing the right thing do not read as regressions — while still asserting that most surfaces *still do not*, so the gap cannot be quietly claimed closed. 35 tests; full suite **24,503**.<br><br>**C29d — partition a parcel to an area. ✅** C27 called this "the classic reason a surveyor opens a calculator at all" and found nothing in the product that could do it. **Bisection, not algebra** — a closed form exists for a cut across a convex quadrilateral and stops existing the moment the parcel has a re-entrant corner or the cut crosses more than two edges, both ordinary. Bisection is sound *only* because the area on one side of a sweeping line is monotonic in how far it has swept, so **monotonicity is asserted rather than assumed**, and the achieved area is verified on a concave parcel and on a cut crossing four edges — the cases where a closed form would have quietly stopped applying. **Two real bugs, both caught by tests written for exactly them.** First, the sweep direction: area *decreases* as the cut moves along the normal, and the solver assumed it rose, so every bisection ran the wrong way and returned the whole parcel. Now the sense is **measured at the brackets** rather than reasoned about, which also means a future change to the keep-side convention cannot silently invert it. Second, and worse: **rotating about a hinge is not monotonic**, and the first version bracketed on the two endpoints of a half turn. Hinged at (40, 0) on a 100 × 100 square, bearing 0 keeps 4,000 and bearing 180 keeps 6,000 — so 3,500 was declared unreachable, while bearing 270 keeps *nothing* and bearing 90 keeps *everything*. Replaced with a 1° sweep of the **full** turn to find a bracketing interval, then bisection inside it where the function really is monotonic; the full turn matters because a line and its reverse are the same line but keep **opposite** sides, so a half turn reaches only one of each complementary pair. A third assumption also proved wrong and the test now records the truth: **a hinge outside the parcel works fine** — a line through a distant point still cuts, and every area remains reachable as it rotates. Impossible targets are refused outright, because returning the whole parcel with a quiet 1-acre "error" would let a bad call through into a deed. The UI reads the live selection (taking selection-as-input to **3 of 15**), accepts acres or square feet since "one acre off the north end" is how the request arrives, takes a closed POLYLINE as well as a POLYGON because imported linework is often never typed as one, and **places the cut line** — a partition whose answer is a number still leaves the surveyor to draw it, and drawing by eye loses exactly the precision just computed. The placed line is stamped with the **achieved** area, never the requested one. It also fills the registry's `AREA` group, which was declared and empty — the C21 shape. 36 tests; full suite **24,541**.<br><br>**C29e — slope, grade and vertical curves. ✅** The last of the four capabilities C27 measured as absent. **One convention, stated once**: grade is a percentage throughout, and the ratio and vertical angle are reported *alongside* it rather than instead — because percentage, ratio, decimal fraction and angle are all used in surveying, and **two of them differ by a factor of 100 while both look entirely reasonable** (2% and 0.02 each read as "a gentle grade"). Run and slope distance are both shown, since run is what a plan carries and slope distance is what a tape measures, and confusing them is a real field error on steep ground. The vertical curve is the standard equal-tangent parabola, and three decisions carry it. **No turning point is reported unless one falls ON the curve** — two grades of the same sign never reverse, the formula still yields an x outside `[0, L]`, and a "high point" 400 ft past the end of the curve is a number somebody would stake; the UI says so in words, because an empty cell reads like the calculation failed. **Stations outside the curve follow the tangents**, which is what the road actually does — extending the parabola sends the profile off in a direction the road never goes, more wrongly the further out it is asked. And the stake-out table always carries **BVC, EVC and the turning point** on top of the interval, since the turning point almost never lands on an even 50 and a profile table missing its high point has to be hand-interpolated at the exact spot drainage depends on. Tests verify the summit by **sampling the curve independently of the formula that produced it**, and check tangency at both ends numerically. The grade half reads the live selection (selection-as-input now **4 of 16**) and **refuses a point with no elevation rather than assuming zero** — a confident lie, and invisible in a selection count. The vertical-curve half types its input **on purpose**: a PVI station and two design grades are not things the drawing holds, they are what the road is being designed *to*. New `PROFILE` group, kept separate from `CURVES` so two different meanings of "curve" do not share a heading. One self-inflicted find: I named the table-interval state setter **`setInterval`**, shadowing the timer API, and the resource-cleanup guard reported an uncleaned interval. Renamed — the guard was right that the name is a hazard. 67 tests; full suite **24,585**.<br><br>**C29f — the two COGO vocabularies (C27's F3), and a real bug underneath. ✅** C27 reported `cogo.ts` and `solver.ts` as duplicate implementations, one unused, and suggested picking a winner. Reading them properly says something more useful: **they are not redundant, and deleting either would have broken `CalcPointDialog`, which imports from both.** The boundary is **the number of answers** — distance–distance and bearing–distance each yield up to *two* points and the surveyor picks, which the `SolverResult` envelope has no shape for; the solver's single-answer constraint solves are what the AI tool-registry forwards without case analysis. Two of the three `cogo` functions C27 flagged as orphaned are reached through `computeCogoSolutions` in the same file. **But exactly one operation existed in both, and they disagreed.** Bearing–bearing: `cogo.brgBrgPoint` rejects a crossing behind either station — with a comment saying why, "a bearing entered backwards would otherwise plant the point on the wrong side of a station" — while `solver.calcPointFromTwoBearings` intersected the *infinite lines* and returned it. **The safer of the two was the one nothing called.** The dialog's bearing–bearing method *and* the AI tool registry both used the permissive one, so a mistyped bearing (a back bearing, a transposed quadrant) placed a point hundreds of feet the wrong way and reported `ok: true`. Fixed in the solver so both callers get it, with a reason that names the likely cause rather than a bare "no intersection" — which is the kind of refusal a surveyor argues with instead of re-reading their field notes. A crossing exactly *at* a station still counts as forward, since refusing it would reject the commonest deliberate case. 14 tests; full suite **24,599**.<br><br>**C29g — spline → arcs, made reachable. ✅** The last engine C27 found exported and called by nothing. **The need is real and is about the deliverable, not the screen**: a spline is a perfectly good curve on a monitor and a problem in a filing, because plenty of the packages a plat gets handed to will not accept one — and the usual fallback, a polyline of hundreds of chords, *is* accepted and is not the same drawing; it prints heavier, edits worse, and every vertex is a place for a rounding difference to appear. A bi-arc fit reproduces the curve to a stated tolerance in a handful of entities, and the fitter was already written. Two pieces were missing: a sampler turning this codebase's `SplineGeometry` into the point list it wants, and a caller. The sampler is **deliberately the same walk the DXF writer already uses** — a second, subtly different one would mean an exported DXF and a converted-in-place spline disagreed about where the same curve is, which is pinned by a test that reads both files. End tangents come from the **control legs rather than the sampled chords**, because the first and last legs *are* the tangents of a cubic bezier and a chord is only approximately one; a handle pulled onto its anchor is legal geometry and falls back to the chord instead of dividing by zero. Reached from the feature context menu, **only on an actual spline** (a greyed row on every other feature would be noise on a menu that long, so `MenuItemDef` gained a `hidden` flag), replacing the spline in **one undo entry** — half a conversion leaves the drawing holding neither the curve nor its arcs — keeping the original layer and style so converting does not also restyle, and recording the achieved deviation because the fit is an approximation. Tests check that segments join end to end (a gap is invisible at drawing zoom and fatal in a deliverable, where the receiving package treats the run as open), that both endpoints of every arc really lie on its fitted circle, and that a tighter tolerance never costs *fewer* segments. **Both orphan ratchets fired correctly again** — the second time in this doc that building the thing an entry was excusing is what made it stale. 22 tests; full suite **24,621**.<br><br>**Still open on C29**: selection-as-input on the remaining twelve surfaces — C27's F6, twelve separate pieces of work rather than one slice, and tracked by the `usesSelection` flag in the registry so the count stays visible. |
| C30 Show the work | ✅ **C27's F1 was half right, and the half it missed is the interesting one.** It said provenance exists on one surface and "the field does not exist on `Feature`". The first part holds; the second does not — `lib/cad/ai/provenance.ts` is a complete five-field model with stamp/read/strip and a right-click "Why did AI draw this?". But it is **AI-scoped** (origin, confidence, prompt hash, batch id), and none of that means anything for a curve somebody solved in a calculator. So the calculated half had nothing — **and by the end of C29 four surfaces had each invented their own vocabulary for it**: `calcSource: 'CURVE_CALCULATOR' | 'SPIRAL_CALCULATOR' | 'PARTITION' | 'SPLINE_TO_ARCS'`, each with ad-hoc `calcRadius` / `calcAreaSqft` / `calcMaxDeviation` neighbours. **That drift was introduced by the slices that were filling the gap**, which is the clearest argument available for doing the model before the next one. `lib/cad/derivation.ts` is one vocabulary, and the split it enforces is the point: **what was GIVEN and what was SOLVED live on opposite sides.** "R 200, L 209.44" tells a reader nothing about which was which, and on a plat under examination that is the only thing being asked — it is the difference between a corner solved from two record calls and a corner that was moved because it looked wrong. The partition surface shows why it matters most: *requested* area is an input and *achieved* is an output, so the miss stays checkable years later. Lives in the properties bag for the same reason `aiOrigin` does — every reader, writer, exporter and undo path already carries `properties` verbatim, and old drawings load without a migration (the C8/C18 conclusion, third time). Owns a **disjoint key set from the AI model**, tested, because both live in the same bag and an overlap would mean stripping one silently damaged the other — a feature that is both AI-authored and calculated is an ordinary thing. Malformed JSON yields an empty object rather than throwing: a hand-edited file must not make a drawing unopenable, and a missing input list is a far smaller problem than a canvas that will not render. `stripDerivation` exists because geometry that has been dragged is no longer what the calculation produced, and leaving the stamp on would make it claim a derivation it no longer has. Read back through a **"How was this made?"** row, deliberately separate from the AI one — they answer different questions with different evidence, and folding them together would mean neither could be as specific as it should be. Four C29 assertions followed their keys to the new home; sentence case, not title case, because these are labels in a list. 28 tests; full suite **24,649**. |
| C31 Merge AI vocabularies | ✅ **D4's premise needed one correction, and it lowers the risk considerably.** D4 read the situation as "76 additions across two registries"; reading both first shows they are **not parallel lists of the same actions**. `tool-registry.ts` holds 13 typed tools with arg schemas and `execute`, and **`claude-proposer.ts` already derives its Anthropic tool list from it** — that path has no hand-maintained duplicate and never had one. `drawing-chat.ts` is a different abstraction: a chat orchestrator whose `EDIT_DRAWING` carries bulk add/delete/modify/transform/fit/hide, described to the model by a **hand-written prompt**, and it did not import the registry at all. So there is no 76-addition duplication to unwind — there is something narrower and still fatal to C34–C36: **the chat path could not reach a registry tool at all**, and adding 38 tools would have left it 38 behind with nothing saying so. The fix is additive. `lib/cad/ai/capabilities.ts` derives the callable list from `toolRegistry` and generates the prompt lines that describe it, so the prose that was the drift source is now generated from the code it describes; a test asserts **no tool name appears as a literal anywhere in the module**, because a hand-written copy is exactly the failure this codebase keeps hitting under other names — 29 tools falling through to the idle prompt (C15), two snap types with no engine (C17), a code-style panel nothing read (C22). The chat gains one `CALL_TOOL` action, validated **while the tool name is still a string in a JSON blob** rather than after the orchestrator has decided the action is worth applying, and validated *again* before execution since an action can also arrive from a replayed conversation. The guard uses `Object.prototype.hasOwnProperty` deliberately, with a test saying why: **`'constructor' in toolRegistry` is true**, so an `in`-based check would have let a model reach a prototype method. Refusals surface the tool's own `{ ok, reason }` verbatim — every tool has already said why in the surveyor's terms, and paraphrasing would be a second wording free to drift from the first, which is the very shape being fixed one level up. A throwing tool is caught so it cannot take the conversation's reply with it. **`EDIT_DRAWING` is untouched**: it expresses bulk edits the registry has no shape for, and rewriting a working 1084-line orchestrator to fold one into the other is the risk D4 calls the highest in the doc, taken for no gain. Two of my own assertions failed on **CRLF** — a bare `\n` in a pattern does not report a line-ending problem, it just fails to match and reads as "the code is missing". 18 tests; full suite **24,667**. |
| C32 Selection as AI scope | ✅ **The selection was already sent; what was missing is the half the slice actually names.** `buildSelectionDigest` has always attached the live selection to every turn, so the AI could see it. "Explicit and visible, not inferred" asks for two other things, and only one is about visibility. **Invisible**: the surveyor cannot see what the AI is about to act on before pressing send — four hundred features from a rubber-band ten minutes ago look exactly like none. **Drifting**, which is worse: the selection is read when the message is *sent*, not when it was composed, so clicking the canvas mid-sentence — to look at the thing being described — silently changed what "these" meant. That is the worst shape of bug this doc has catalogued: the request was right, the answer was right *for a different question*, and nothing looked wrong afterwards. So the scope can be **pinned**, and is always shown. `lib/cad/ai/scope.ts` summarises it as one line the surveyor reads while still typing — count, types busiest-first (40 points and one line leads with the points), and layers truncated rather than wrapped because the chip is one line by design and a nine-layer spread is a fact needed at a glance, not a list to read. **A pinned scope outlives the features in it**: pin twelve, delete four, send — and the count reflects eight while the chip *reports* "4 gone" rather than silently shrinking, because quiet correction would mean "move these twelve" moves eight with the chip agreeing with itself throughout. Pinning an empty selection is refused, so the chip has two honest states instead of a third claiming a scope of nothing; the ids are copied rather than referenced, since a caller holding a live array would reintroduce the exact drift being fixed; and the pin is **deliberately not persisted** — restoring one from last week would scope a fresh request to features the surveyor has long forgotten choosing, which is this slice's own failure put back in a worse form. Unpinning is one click, because a mode you can enter and not leave is C26's isolate bug one panel over. 21 tests; full suite **24,688**. |
| C33 Layer as AI scope | ✅ **"Same, for a layer" turned out to be a different kind of scope, and that difference is the whole slice.** C32's pin is a frozen list of ids, which is exactly right for "these twelve". Freezing a *layer* would be wrong in a way that is very hard to notice: a surveyor scopes to FENCE, draws three more fence lines while thinking, then says "shorten these" — and with a snapshot the three new ones are silently excluded **while the chip has said "Layer: FENCE" the entire time**. The scope would be lying by omission about the one thing it exists to state. So `ScopeRef` became a discriminated union and a LAYER scope **resolves live on every turn**: it picks up features drawn after it was chosen and drops ones moved off. Three refusals follow from that, each tested. A layer that **no longer exists** resolves to empty rather than falling through to the live selection — the fallback would silently act on a completely different set while the chip still named the deleted layer. Same for a call with no document: not "everything on the layer" but *nothing knowable*. And **staleness belongs to IDS only** — a layer scope resolves live, so it has nothing to have lost, and reporting "3 gone" after an ordinary deletion would describe normal editing as a problem with the scope. The two kinds also disagree deliberately on emptiness: an **empty layer is a legitimate scope** ("everything on DEMOLITION" is meaningful for a layer about to be drawn on) while an empty selection is not, so the chip says the layer name and its count instead of "Nothing selected". Reachable from the layer context menu, which also opens the panel — scoping to a layer and then having to go find the AI panel would leave the surveyor unsure it took. Four C32 assertions followed the shape change; one of mine failed twice on a regex window too narrow to clear its own explanatory comment, and was replaced with a slice. 18 tests; full suite **24,706**. |
| C34 AI reach: drawing tools | ✅ **Registry 13 → 17, and the parametric point is the whole slice.** The AI had three ways to create geometry — a point, a line, and a polyline/polygon through vertices — so a rectangle, a circle, an arc or a text note could only be reached by handing it a vertex list and asking it to approximate. That is worse than "a bit rough": **a circle emitted as 64 vertices is not a circle.** It exports as a polyline, cannot be snapped to a centre, and `computeFeatureArea` measures the inscribed polygon — quietly under by ~0.4% at 64 sides, exactly the size of error that survives into an acreage. The parametric geometry this codebase already stores (`geometry.circle`, `.ellipse`, `.arc`) exists so that does not happen, and the AI had no way to produce it. `drawRectangle` / `drawCircle` / `drawArc` / `drawText` fix that, each **refusing rather than drawing something degenerate**: a rectangle with no width is a line the caller did not ask for; three collinear points have no arc, and emitting one of infinite radius renders as nothing and reads as a silent failure; empty text places a feature the surveyor cannot see, cannot select by looking, and will not know to delete. `drawArc` **measures its sweep direction from where the middle point actually lies** rather than assuming — the same 300-foot major-arc trap C29 hit by reasoning about a convention instead of reading it — and a test verifies the swept range contains that point independently of the flag that produced it. `drawText` writes `fontSize` onto `properties`, where C20 established the canvas and the PDF writer read it. **C31 made this cheap and this is the slice that collects on it**: the tools appear in the chat prompt and in `claude-proposer`'s SDK tool list with no prompt edit and no second registration — two pre-existing tests pin the exact tool list, and that *both* changed from one edit is the evidence the derivation holds. 33 tests; full suite **24,739**. |
| C35 AI reach: modify tools | ✅ **Registry 17 → 22. Every AI tool up to here CREATED something** — the AI could draw a fence and could not move it. So "do this to these", the whole point of C32/C33's scope work, had nothing on the other side of the sentence: the scope named features and the vocabulary could only add more. `moveFeatures` / `rotateFeatures` / `scaleFeatures` / `mirrorFeatures` / `deleteFeatures`, and two decisions run through all five. **They take ids, not "the selection."** The store's existing operations act on the live selection, which is the wrong coupling for a tool — the AI would act on whatever the surveyor happened to have highlighted at execution time rather than on the scope the request named, which is **the exact drift C32 spent a slice removing, reintroduced one layer down**. And **all-or-nothing**: any missing id or locked layer refuses the whole call, because a partial modify is the worst outcome available here — moving 38 of 40 leaves a state nobody asked for, and the two left behind are invisible against 38 that moved. **One undo entry per call**, tested at forty features, which establishes C37's requirement here rather than retrofitting it; a per-feature entry leaves the surveyor pressing undo forty times and wondering when to stop. Refusals where the operation would otherwise corrupt rather than fail: a scale factor of 0 collapses geometry to a point and a negative one mirrors it while claiming to scale; two identical mirror-axis points define no line and divide by zero, scattering geometry to NaN — placed nowhere, and far harder to notice than a refusal. Rotation is CCW-positive, stated and tested because both conventions are in use and the wrong one mirrors the result while every distance stays correct. The transforms are the **same helpers the manual tools use**, so an AI move and a dragged move produce identical geometry. Both pinned tool-list tests changed again from one edit — C31's derivation, holding for the second slice running. 34 tests; full suite **24,773**. |
| C36 AI reach: measurement + annotation | ✅ **Registry 22 → 25, and it found a live bug in the area engine.** The ask was "AI integrated with all measurements"; the registry could inverse between two points and that was the whole of it — the AI could not answer "how big is this parcel" or "how long is that fence" about geometry already on the drawing, only compute from numbers it was handed. (Annotation was already done: `drawText` shipped in C34.) `measureFeature` / `measureTotalArea` / `describeFeature` are the first **read-only** tools in the registry, and the tests pin that literally — no feature changes, no undo entry — because a measurement showing up in the undo stack would be the least expected failure available here. Two reporting decisions do real work. An open shape reports **null area, not zero**: zero is a legitimate measurement and "this has no area" is a different statement, and a model handed 0 will happily add it into a total. And `measureTotalArea` **names what it skipped** rather than absorbing open features silently — a total quietly missing three polylines is one the surveyor cannot reconcile against the drawing, and it is wrong in the safe-looking direction, smaller and still plausible. A POLYGON's perimeter closes and a POLYLINE's run does not, tested on the same four vertices (300 vs 250), because measuring both alike turns a perimeter into a perimeter-minus-one-side — a number that still looks entirely right on a five-sided tract. `describeFeature` is the LIST equivalent and surfaces **C30's derivation**, which is what a surveyor actually asks the AI when checking a plat. **The bug**: `computeFeatureArea` measured a "circular polygon" — a POLYGON carrying parametric `circle` data and no vertices, which is this codebase's own established representation, drawn as a circle by the renderer and emitted as a DXF `CIRCLE` — as **zero square feet**, because it went straight to the shoelace over an empty vertex list. Found by the new measurement tool on geometry C34 had just taught the AI to create. **A circle reporting no area is the kind of wrong that reaches an acreage: it is not an error, it is a number.** Fixed for circles and ellipses alike. 22 tests; full suite **24,795**. |
| C37 AI action = one undo | ✅ **The batch walker could only see tools that CREATE.** `aiBatchIdFromEntry` read the id off the *features* an entry produced, which works for `addPoint` and works for nothing C35 added: move / rotate / scale / mirror push `MODIFY_FEATURE` batches and delete pushes `REMOVE_FEATURE`, and none of them create a feature to carry a stamp. So an AI request that moved forty features produced an entry the walker returned `null` for, and `undoMostRecentAIBatch` stopped dead at it — one press, thirty-nine features still moved, and the stack looking for all the world like the undo had worked. The machinery C37 was meant to "prove holds" did not hold. The fix moves the id onto the **entry** (`UndoEntry.aiBatchId`), and that placement is the decision: stamping AI provenance onto geometry the surveyor drew would claim an authorship the AI does not have — it moved the fence, it did not draw it — and a delete leaves nothing behind to stamp at all. The feature scan stays as a fallback so pre-C37 entries keep working. Then the reach question: threading `aiBatchId` through twenty-five tool signatures would leave tool twenty-six unlabelled and silent about it, so `runAsOneAIBatch(fn)` wraps the **call site** instead — it notes the top of the stack, runs the call, and labels every entry that appeared, which covers the whole registry and everything added to it later. The chat executor now wraps `CALL_TOOL` in it. The id is deliberately **not** in any tool's `inputSchema` (same treatment `provenance` and `sandbox` already get): a model free to invent an id is free to reuse one, and two unrelated turns sharing an id would make "undo that AI request" swallow the earlier one too. The five modify tools still accept an explicit id for callers driving them directly, and the wrapper only labels entries that lack one, so the two compose. Tests pin the contract at both ends — a four-tool turn reverses to the surveyor's original coordinates in one call, and a manual edit interleaved on top **stops** the walk rather than being reached past, because undo is a stack and popping out of order corrupts everything above it. 13 tests; CAD suite **4,316**. |
| C38 Preview before apply | ✅ **Accept said it worked and nothing happened, for nine of the fourteen writing tools.** `executeProposal` was a `switch` over five hand-listed tool names with no `default`. TypeScript read it as exhaustive because `ProposalToolName` *was* those five names; the runtime disagreed, because `blockToProposal` casts any non-solver registry tool into that union. So a `drawRectangle` or `deleteFeatures` proposal reached the dispatcher, matched no case, and fell out the bottom returning `undefined` — and the card's `if (result && !result.ok)` reads `undefined` as "no error" and logs *"✓ Applied"*. The root cause is a direction: the five **writers** were enumerated and everything else derived by subtraction, so every tool added after them fell out of the proposal type by default. C38 flips it — `READ_ONLY_TOOL_NAMES` (the eight solvers plus C36's three measurements) is the enumerated list and `ProposalToolName = Exclude<ToolName, ReadOnlyToolName>`, so a tool added tomorrow is reviewable **by default** and the cost of forgetting the list is a needless card rather than an unreviewed write. The dispatcher then goes generic over the registry, which removes the class rather than the instance: there is no list left to fall off, and an unknown name refuses out loud instead of returning nothing. The same flip fixed a second miss — `blockToProposal` gated on `isSolverTool`, which named eight tools while the type it guarded meant "everything but the original five", so C36's measurements were in neither bucket and a `measureFeature` call came back as a card offering to *apply* a measurement; they now run inline and return their answer to the model, which is what lets its next tool call be right. Then the preview itself: the ghost builder lived inside `CopilotCard.tsx` and knew a point, a line and a polyline. It moves to `lib/cad/ai/preview.ts` — pure, document-in/shapes-out, no store reads, which is what lets it be tested against real geometry in Node — and covers all fourteen. Two rules run through it. **A modify preview shows the RESULT**: "rotate these forty features 3°" is meaningless as a highlight of what is already on screen, so the ghost is drawn where the geometry will end up and accepting confirms something seen rather than betting on an adjective. **A delete preview shows what will GO** — painting nothing would have left the registry's one irreversible-feeling proposal as the only one with no preview at all. The channel now carries a list (single-shape senders still work), curves are sampled at 64 segments for the ghost while the real geometry stays parametric, and an arc through collinear points ghosts *nothing* because the tool refuses it — previewing geometry Apply will never produce is its own kind of lie. 23 tests; CAD suite **4,339**. |
| C39 Reach measurement + guard test | ⬜ |
| C40 Payroll S9c | ⬜ |
| C41 Payroll S8 auto-transfer | ⬜ **owner-gated** |
| C42 Research S-8 | ⬜ |
| C43 Mobile background uploads wiring | ⬜ **owner-gated** |
| C44z fieldbook_notes name collision (schema decision) | ⬜ |
| C44a Enumerate integration points | ⬜ |
| C44b Orphan check + ratchet | ⬜ |
| C44c Drive each integration end to end | ⬜ |
| C44d Failure paths are legible | ⬜ |
| C44e Integration documentation pass | ⬜ |
| C44 Full re-sweep 1440 + 390 | ⬜ |
| C45 Drive new surfaces at 390 | ⬜ |
| C46 Functional sweep | ⬜ |
| C47 Green gate | ⬜ |
| C48 Reconcile seeds vs live DB | ⬜ |
| C49 Apply outstanding seeds | ⬜ |
| C50 Verify against live schema | ⬜ |
| C51 Green gate before merge | ⬜ |
| C52 Push + merge to main | ⬜ **confirm with owner first — irreversible** |

---

## Working notes for whoever picks this up

1. **`MSYS_NO_PATHCONV=1` on every audit invocation.** Without it Git Bash rewrites `/admin/cad`
   into a Windows path and the run reports one bogus `load` finding — which looks exactly like a
   broken page.
2. **Never edit source while a sweep is running against a dev server.** Next recompiles and the
   sweep records fake 500s. Run long sweeps against `npm run build && next start` on a separate
   port; a production server is immune to edits.
3. **A JSX comment cannot sit beside the root element of a `return (`** without a fragment. This
   cost one bogus 500 and a "findings=0" that looked like success during A14.
4. **Re-probe before believing a finding.** A 500 with `findings=0` is not a clean page; it is a
   page that never rendered.
5. **Read the findings before fixing any of them.** In the previous pass roughly half of every
   opening number was the instrument. The 28 CAD findings fixed in A14 were 3 real defects.
6. **Drive the surface, don't only measure it.** The snap-popover clip found in A14 was invisible to
   all six audit rules at every width — it was found by reading the CSS, and it had been shipping.
