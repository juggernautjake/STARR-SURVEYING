# Handoff — 2026-08-02

Stopping point for a machine restart. Everything below is committed locally.

---

## 1. Read this first: the branch situation

**We are on `main`, not the feature branch.** Partway through the session the other CLI session
merged its work — and all of the research work with it — into `main` (`cf2454f1d`), and the working
tree came with it. Commits since then have landed on `main` directly.

**Nothing has been pushed.** The standing instruction to hold the push until everything is built and
verified still applies, and has been honoured throughout.

Every research commit survived the merge intact. `git log --oneline -30` shows them.

---

## 2. The immediate next task

**14 tests fail in `__tests__/recon/phase13-statewide-adapters.test.ts`.**

They are **stale, not broken by the new work**. They assert that `getClerkAdapter` returns
`HenschenClerkAdapter` for Kimble, `IDocketClerkAdapter` for Rockwall and `FidlarClerkAdapter` for
Ward — routing that was deliberately gated off in R37/R38 because **every one of those vendors' URLs
is dead**, browser-confirmed:

```
Henschen   16/16  ERR_NAME_NOT_RESOLVED
iDocket    18/18  HTTP 404
Fidlar      6/6   ERR_NAME_NOT_RESOLVED
```

Those counties now fall through to TexasFile, which works. The tests encode the pre-R37 belief that
the adapters were reachable.

**Fix**: update them to assert the current, correct routing — the same treatment already applied to
`clerk-registry-claims.test.ts` and `vendor-reachability.test.ts`. Do **not** re-enable the vendors
to make the tests pass; that would restore routing to dead domains, and the failure surfaces as "no
records found", which is a statement about the property rather than about our routing.

Note also: these live in the **root** suite (`__tests__/`), not the worker suite
(`worker/src/__tests__/`). Running `npx vitest run` from `worker/` will not see them. The root suite
is ~21,900 tests; the worker suite is ~880.

---

## 3. Where the work got to

### County coverage: 7 → 22 routed counties, six vendors

| Vendor | Counties | Notes |
|---|---|---|
| Kofile | 20 | Bell, Travis, Milam, Brazos, Leon, Madison, Walker, Montgomery… |
| Tyler Eagle | 9 | McLennan (Waco), Burnet, Hamilton, Hill, Mills, Erath, Navarro, Somervell, Williamson |
| eDocTec | 2 | Coryell (Gatesville + Copperas Cove), Lampasas |
| Avenu 20/20 | 2 | Falls, Robertson |
| Aumentum | 1 | Bastrop |
| iDocMarket | 1 | Bosque |

Three of those vendors had no name in this codebase when the session began.

**Every county in the 80-mile ring is now classified** — routed, credential-blocked, or with a
recorded reason. Lee and San Saba publish nothing online at all; Hays has no located portal;
Limestone needs a CountyFusion login.

### Search-by-land works everywhere

`searchByLegalDescription` is implemented on all routed vendors. The Kofile one had been returning
`[]` with a "not supported" log across **twenty counties including Bell** — the single search a
surveyor most wants, answered with a confident silent nothing.

### The central defect, now structurally prevented

"An unknown rendered as an answer" was found and fixed **eleven times in the clerk adapters, six in
the CAD adapters, three in the adjoiner worker, and three in the pipeline**. Two ratchets now fail
the build if it returns:

- `worker/src/__tests__/no-silent-empty-results.test.ts` — twelve routed adapters
- `worker/src/__tests__/adjoiner-failures-surface.test.ts` — the adjoiner worker's error drain

### New this session, from the owner's mid-session requests

- **`GloLandGrantAdapter`** (S-6) — Texas GLO, free, no login. Bell returns 1,523 land grants with
  free PDFs. This is the *original survey* layer every legal description is written against, and the
  platform had nothing from it.
- **`document-identity.ts`** (S-12) — cross-vendor document identity so a paid pass never re-buys
  what a free pass already got. **When identity is uncertain, it buys**, because a false match omits
  a document invisibly while a false miss costs a few dollars and shows in the ledger.
- **`research-modes.ts`** (S-11) — free/paid run modes, free sources first in *both*, concurrency
  capped at 4, progress reporting, and escalation advice that refuses to oversell paying.

---

## 4. Next slices, in order

| Slice | What | Blocked on |
|---|---|---|
| — | **Fix the 14 stale recon tests** (§2) | nothing — do this first |
| S-7 | texaslandrecords / Avenu aggregator — 23 counties, free index | nothing |
| S-13/S-14 | Wire `DocumentIndex` + `buildPlan` into the real pipeline | nothing |
| S-8 | `vendor_accounts` schema + balance tracking | nothing |
| S-9 | Stripe card-on-file + auto top-up | **owner: amounts, ceiling** |
| S-10 | Purchase-ledger reconciliation | S-8 |

---

## 5. Owner decisions still open

Asked several times; still the only things that can close
`RESEARCH_PLATFORM_DEEP_BUILD_2026-08-02.md`.

1. **TexasFile subscription — yes/no.** Covers all 254 counties and closes every remaining gap.
2. **Auto top-up numbers** — top-up amount, low-water threshold, monthly ceiling.
3. **Which vendors** to open accounts with.
4. **Imagery licensing** — provider, and whether tiles may be stored in a customer packet.
5. **Captcha/ToS posture** — which counties may be automated.
6. **Golden-record properties** — a few known-good properties to validate runs against.

---

## 6. Site verdicts from the owner's links

| Site | Verdict |
|---|---|
| **glo.texas.gov** | ★ Build on it. Free, authoritative, original surveys. **Done** (S-6). |
| **texaslandrecords.com** | Avenu, 23 counties, free index. Worth an adapter (S-7). |
| **texasfile.com** | The right paid subscription — 254 counties. Needs decision. |
| **tsl.texas.gov** | Reference, not a records system. Its map collections are real secondary evidence. |
| **publicrecordsdata.us** | **Do not integrate.** Its own header says it is not government-affiliated; pricing runs $1 → $30 → $720, a trial converting to a subscription. It is a data broker reselling county records we already read directly — not authoritative, cannot produce a certified copy. |

---

## 7. State

- **Branch**: `main` (see §1). **Nothing pushed.**
- **Worker suite**: 880 passing.
- **Root suite**: 21,895 passing, **14 failing** (the stale recon tests in §2).
- **Seeds applied to production**: through 568.
- **Uncommitted in the tree**: another session's files (`__tests__/dnd/`, `docs/planning/in-progress/SLOT_DRIVEN_*`, `scripts/_tmp_slotcheck.mjs`). **Not mine — leave them alone.**
