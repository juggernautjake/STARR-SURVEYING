{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set granted to the main window in Slice T2 — the absolute minimum for the dev shell. Slice T4 adds dialog/fs permissions for native file open/save; Slice T6 adds path/fs scopes for the autosave directory; Slice T7 adds menu/window permissions for the native menu wiring.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
