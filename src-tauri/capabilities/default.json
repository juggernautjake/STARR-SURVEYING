{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set granted to the main window. Slice T2 shipped the core baseline. Slice T4 adds dialog:allow-open + fs:allow-read-text-file for native file open. Later slices: Slice T6 adds a path scope for the autosave directory; Slice T7 adds menu/window permissions for the native menu wiring.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:allow-read-text-file"
  ]
}
